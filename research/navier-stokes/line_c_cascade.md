# Line C — Tao's cascade program: engineered blowup, and what blocks porting it to true Navier-Stokes

Status legend (one tag per claim): **PROVED-HERE** (complete proof written out, every step checkable), **KNOWN** (cited, not reproved), **COMPUTED** (script plus printed output below), **CONJECTURE**, **FAILED** (attempted, with the exact breaking step). Citation tags: **[checked via WebSearch]** / **[unverified]** as in `00_problem.md`. All no-go items `2.x` and transfer tests `T1`-`T6` are cited by number from `00_problem.md` and are not re-derived here.

## Goal

Tao's 2016 program (no-go item 2.2) has two halves. The first is a *mechanism*: a cascade in which a quadratic, energy-conserving, scale-invariant nonlinearity moves an order-one fraction of the energy from scale `2^n` to scale `2^(n+1)` in a time that shrinks geometrically fast enough to beat viscous damping, so that infinitely many transfers happen before a finite time `T*`. The second is a *carrier*: a bilinear operator `B~` that supports the mechanism while retaining every property of the true Euler nonlinearity `B(u,v) = -P((u.grad)v)` that the known regularity theory uses. This line does the first half concretely on ODE (dyadic / shell) models, measures where it stops, and then states precisely which structural property of `B` blocks the second half for the true equation. The intended output is not "a blowup"; it is a quantitative location of the boundary between the cascade regimes, and a list of the exact properties an engineered nonlinearity must break.

Line A of this project asked whether a coercive Lyapunov functional could rule out blowup and found that none of the natural candidates works; this line asks the complementary question, whether the cascade that would produce blowup can be built. In the one-mode-per-scale class it does not beat the true dissipation in any run performed here (C7, C8); this line measures by how much it falls short and identifies the structural reason. The literature reports that the corresponding statement about the untruncated model is a theorem — Barbato-Morandin-Romito 2011, for positive solutions of the viscous dyadic model — but the exponent range and solution class of that theorem were not read anywhere in this project (`00_problem.md` section 0), and under this file's own inferred `alpha = 2 s / 5` dictionary Cheskidov's stated *global*-regularity threshold `alpha >= 1/2` translates to `s >= 5/4` and so does not reach `s = 1` (only his local-regularity range `alpha > 1/3`, i.e. `s > 5/6`, contains it). So the global-regularity statement for the untruncated model at `s = 1` is carried here as **CONJECTURE** supported by COMPUTED evidence on truncated systems, not as KNOWN; the citation dependence is spelled out at the point of use in C7.

## Setting and notation

The dyadic (shell) model studied here is, for `n = 0, 1, 2, ...` with the conventions `a_{-1} = 0` and `a_N = 0` at a truncation to `N` shells,

```text
da_n/dt = -nu * 2^(2 s n) * a_n  +  lam^n * a_{n-1}^2  -  lam^(n+1) * a_n * a_{n+1},     lam = 2^(5/2).
```

`a_n(t)` is the amplitude of the `n`-th dyadic shell in an `L^2`-normalized basis, so that `E = sum_n a_n^2` is the energy analogue, `H^1 = sum_n 4^n a_n^2` is the enstrophy analogue, `k_n = 2^n` is the physical wavenumber, and `s = 1` (dissipation `4^n = k_n^2`) is the true Navier-Stokes Laplacian. The nonlinear coefficient base `lam = 2^(5/2)` is not a free choice: C2 below shows it is the unique base for which the model has the exact Navier-Stokes scaling symmetry of `00_problem.md` section 1.6 together with the dissipation `4^n`.

Two normalizations appear in the literature and they must be kept apart. Writing the model in "flux units", with `K_n := lam^n` as the coefficient of the nonlinearity and the dissipation as `nu * K_n^(2 alpha)`, the exponent `alpha` is the one used by Katz-Pavlovic and Cheskidov; writing it in "physical units", with dissipation `nu * 2^(2 s n) = nu * k_n^(2 s)`, the exponent `s` is the one used here and in the Lions/hyperdissipation literature (`00_problem.md` item 2.10). The dictionary is `2 s n = 2 alpha * (5/2) n`, i.e. `alpha = 2 s / 5`:

| quantity                                                                                | this file (`s`, dissipation `2^(2 s n)`) | flux units (`alpha`, dissipation `lam^(2 alpha n)`) | ratio to critical |
| --------------------------------------------------------------------------------------- | ---------------------------------------- | --------------------------------------------------- | ----------------- |
| Cheskidov's stated blowup boundary `alpha < 1/3`, translated by the inferred dictionary | `s = 5/6 ~ 0.8333`                       | `alpha = 1/3`                                       | `2/3`             |
| true 3D Navier-Stokes dissipation                                                       | `s = 1`                                  | `alpha = 2/5`                                       | `4/5`             |
| energy criticality = Lions exponent                                                     | `s = 5/4`                                | `alpha = 1/2`                                       | `1`               |

The identification of Cheskidov's `alpha` with `2 s / 5` is **inferred**, not read off the paper: it is forced by two independent consistency checks, namely that his global-regularity threshold `alpha >= 1/2` coincides with the energy-criticality exponent computed in C2 below, and that his remark "`alpha = 1/3` enjoys the same estimates as 4D Navier-Stokes" matches the ratio `alpha / alpha_crit = 2/3`, which is exactly the ratio `1 / ((d+2)/4)` at `d = 4`. Note the last column: the dyadic model with the true `4^n` dissipation sits at `4/5` of critical, exactly where true 3D Navier-Stokes sits, which is the sense in which the model is scale-faithful.

Because that dictionary is inferred and because no paper was read (`00_problem.md` section 0), every use below of a Cheskidov or Barbato-Morandin-Romito threshold is flagged where it is used, and **no claim in this file rests on one**: the thresholds are quoted only as pointers for a reader with library access, and every statement of this file that could be confused with one of theirs carries the status of its weakest input.

Two profiles will be compared throughout. The **scale-invariant profile** `a_n = c 2^(-n/2)` is the fixed point of the model's scaling map (C2). The **constant-flux (K41) profile** `a_n = c 2^(-5n/6)` is the one that makes the shell-to-shell energy flux `lam^(n+1) a_n^2 a_{n+1}` independent of `n` (C5). The second is strictly steeper than the first, and C10 argues — as a scaling heuristic (CONJECTURE), around a PROVED-HERE power-law core — that this one inequality is the reason the model does not blow up at `s = 1`.

## Results

### C1. Exact energy conservation (inviscid) and the energy identity — **PROVED-HERE**, confirmed **COMPUTED**

Proof. With `a_{-1} = 0` and `a_N = 0`, and all sums over `0 <= n <= N-1`,

```text
(1/2) d/dt sum_n a_n^2 = sum_n a_n (lam^n a_{n-1}^2 - lam^(n+1) a_n a_{n+1})
                       = sum_n lam^n a_{n-1}^2 a_n  -  sum_n lam^(n+1) a_n^2 a_{n+1}.
```

Re-index the first sum by `m = n-1`: it becomes `sum_{m=-1}^{N-2} lam^(m+1) a_m^2 a_{m+1}`, whose `m = -1` term vanishes because `a_{-1} = 0`, and whose remaining terms are exactly the second sum (its `m = N-1` term vanishes because `a_N = 0`). The two sums cancel term by term. With the dissipation restored, `(1/2) d/dt sum_n a_n^2 = -nu sum_n 2^(2 s n) a_n^2 <= 0`. QED. Script part 1a verifies the inviscid telescoping symbolically for `N = 8` with `lam` free; part 1b verifies the viscous identity with `lam`, `nu` and `s` all free. (The earlier version of this sentence described part 1b as using the dissipation `lam^(2n)`, i.e. the special case `s = 5/2`; the script now uses `2^(2 s n)` with `s` symbolic. The dissipation is diagonal, so it plays no part in the telescoping either way.)

This is the property that makes the model a legitimate carrier of Tao's mechanism: it has the exact energy identity of `00_problem.md` item 2.2, so no argument that uses only the energy identity can distinguish it from Navier-Stokes.

### C2. `lam = 2^(5/2)` is the unique base with the Navier-Stokes scaling; the scale-invariant profile — **PROVED-HERE**, confirmed **COMPUTED**

Proposition. Fix the dissipation `2^(2 s n)` and work on the **bi-infinite lattice `n in Z`** — equivalently, at interior shells `n >= 1` of the half-lattice; the boundary shell `n = 0` is a genuine exception and is treated immediately below. Define `b_n(t) := mu a_{n+1}(sig t)`. Then `b` solves the model with the same `nu` and `s` whenever `a` does, if and only if `sig * 2^(2 s) = 1` and `sig * lam / mu = 1`; i.e. `sig = 2^(-2 s)` and `mu = lam * 2^(-2 s)`.

Proof. Differentiate: `db_n/dt = mu sig (da_{n+1}/dt)(sig t)`. Substituting the equation at shell `n+1` and rewriting `a_{n+1} = b_n/mu`, `a_n = b_{n-1}/mu`, `a_{n+2} = b_{n+1}/mu` gives `db_n/dt = -nu sig 2^(2s) 2^(2 s n) b_n + (sig lam / mu) lam^n b_{n-1}^2 - (sig lam / mu) lam^(n+1) b_n b_{n+1}`, and matching coefficients with the model at shell `n` gives the two stated conditions. QED (script part 1c: residual is `0` after substitution).

Boundary shell (correction forced by the round-1 referee report). On the half-lattice fixed in "Setting and notation", with `a_{-1} = 0`, the Proposition is **false at `n = 0`** without the hypothesis just added: the model at shell `0` has no inflow term, but `db_0/dt = mu sig (da_1/dt)(sig t)` contains `mu sig lam a_0(sig t)^2 = mu^2 a_0(sig t)^2` (using `sig lam / mu = 1`), so the residual at the boundary shell is exactly `mu^2 a_0^2` — nonzero for every solution started from this file's own initial condition `a_0 = 1`. Script part 1c now prints both residuals: `0` at the interior shell `n = 3`, and `2^(5 - 4 s) A0(sig t)^2 = mu^2 a_0^2` at `n = 0`. The earlier version of this Proposition omitted the hypothesis and part 1c checked only an interior shell. Nothing downstream changes, because every later use of the map is asymptotic in `n`.

Corollary (uniqueness of `lam`). At `s = 1` the first condition forces `sig = 1/4`, which is the parabolic time scaling `t -> t/4` of `u_lam0(x,t) = lam0 u(lam0 x, lam0^2 t)` at `lam0 = 2`. The same map multiplies each shell amplitude by `lam0^(1 - 3/2) = 2^(-1/2)` (because `||u_lam0||_{L^2} = lam0^(-1/2) ||u||_{L^2}` in `d = 3`, `00_problem.md` 1.6), so its inverse — which is the map `b` above, carrying the content of shell `n+1` to shell `n`, i.e. towards coarser scales — must have `mu = 2^(1/2)`. Then `mu = lam/4 = 2^(1/2)` forces `lam = 2^(5/2)`. QED.

Corollary (scale-invariant profile). `a_n = c 2^(-n/2)` is the unique (up to `c`) profile fixed by the map `a -> b` at `s = 1`, since `2^(1/2) c 2^(-(n+1)/2) = c 2^(-n/2)`. It is **not** a stationary solution: substituting it into the nonlinearity gives residual `-2 c^2 2^(3n/2)` (script part 1e, exact). Under this map, on the bi-infinite lattice, `E -> 2E` and `H^1 -> H^1/2`; on the half-lattice the exact identities are `E(b) = 2(E(a) - a_0^2)` and `H^1(b) = (H^1(a) - a_0^2)/2`, immediate from `b_n = mu a_{n+1}` with `mu^2 = 2` at `s = 1`. Either way this reproduces the supercriticality of the energy and the subcriticality of the enstrophy of `00_problem.md` 1.6.

### C3. The `(nu, amplitude)` collapse — **PROVED-HERE**, confirmed **COMPUTED**

Proposition. If `a` solves the model with viscosity `nu` then `b_n(t) := mu a_n(mu t)` solves it with viscosity `mu nu`, for every `mu > 0`.

Proof. `db_n/dt = mu^2 (da_n/dt)(mu t) = mu^2 [-nu 2^(2 s n) a_n + lam^n a_{n-1}^2 - lam^(n+1) a_n a_{n+1}](mu t)`. The nonlinearity is exactly quadratic, so it equals `-(mu nu) 2^(2 s n) b_n + lam^n b_{n-1}^2 - lam^(n+1) b_n b_{n+1}`. QED.

Consequences used below: only the ratio `amplitude / nu` matters, so the mandatory `(nu, amplitude)` table is one-dimensional; `max H^1` scales by `mu^2` and the stall shell `n_d` (C8) is unchanged; and "blowup for large data at fixed `nu`" and "blowup for fixed data at small `nu`" are the same statement. Verified numerically in part 3 at `mu = 3` over three decades of `nu`.

### C4. The exact self-similar blowup of the inviscid model has infinite energy — **PROVED-HERE**, confirmed **COMPUTED**

Proposition. On the bi-infinite lattice `n in Z`, `a_n(t) = lam^(-n) / ((lam^2 - 1)(T - t))` solves the inviscid model exactly, for every `T`.

Proof. Write `c = 1/(lam^2-1)`. Left side: `c lam^(-n) (T-t)^(-2)`. Right side: `lam^n c^2 lam^(-2(n-1)) (T-t)^(-2) - lam^(n+1) c^2 lam^(-n) lam^(-(n+1)) (T-t)^(-2) = c^2 (T-t)^(-2) (lam^(2-n) - lam^(-n)) = c^2 (lam^2 - 1) lam^(-n) (T-t)^(-2)`. The two agree iff `c = c^2 (lam^2-1)`, which is the definition of `c`. QED (script part 1g: residual `0`).

Its energy `sum_{n in Z} a_n^2` diverges geometrically as `n -> -infinity`, and it violates the boundary condition `a_{-1} = 0` of the half-lattice model, so it is not a solution of the model actually integrated here. Reading (not a correspondence, and weaker than the earlier version of this sentence claimed): the exactly self-similar blowup of this model exists but is not in the finite-energy class, so a finite-energy blowup of the model must be a *front* rather than an exact self-similar profile. That is the same shape of conclusion as no-go item 2.3 (Necas-Ruzicka-Sverak, Tsai), but it is not an instance of it: their exclusion is by the behavior of the profile at small scales and in `L^3`, while the divergence here is at the *large* scales `n -> -infinity`, and the model has neither a velocity field nor an `L^3` norm. The algebra above is PROVED-HERE; this paragraph is a reading. The profile is also the far precursor tail seen numerically in C6 (measured slope near `-5/2 = log2(lam^(-1))`).

### C5. The constant-flux (K41) profile is an exact stationary solution and is steeper than scale-invariant — **PROVED-HERE**, confirmed **COMPUTED**

Define the shell-to-shell flux `Pi_n := lam^(n+1) a_n^2 a_{n+1}`, so that the shell energy budget reads `(1/2) d/dt a_n^2 = Pi_{n-1} - Pi_n - nu 2^(2 s n) a_n^2` (immediate from the equation; it is the term-by-term version of C1).

Proposition. `a_n = c 2^(-5n/6)` makes `Pi_n = 2^(5/3) c^3` independent of `n`, and on the bi-infinite lattice it is an exact stationary solution of the inviscid model.

Proof. `Pi_n = 2^(5(n+1)/2) c^3 2^(-5n/3) 2^(-5(n+1)/6) = c^3 2^(5n/2 - 5n/3 - 5n/6) 2^(5/2 - 5/6) = c^3 2^(5/3)`, since `5/2 - 5/3 - 5/6 = 0`. Stationarity is then `Pi_{n-1} - Pi_n = 0`, which holds for all `n in Z`; on the half-lattice it fails only at `n = 0`, where `Pi_{-1} = 0` and energy must be injected. QED (script part 1f, exact).

The comparison that drives everything below: `2^(-5n/6)` decays strictly faster than the scale-invariant `2^(-n/2)` of C2, since `5/6 > 1/2`.

### C6. Inviscid model: the front reaches infinity in a finite, truncation-independent time — **COMPUTED**; the profile at that instant — **FAILED** (attempted; breaking step recorded below)

Data from `a_0 = 1`, all other shells zero, `nu = 0`, Radau with `rtol = 1e-9`, `atol = 1e-16`; `T_esc(N)` is the time at which the last retained shell `a_{N-1}` first reaches `esc_tol = 1e-9` (script part 2). Two `H^1` columns are printed, and **neither is a diagnostic**; the reason is given below the table.

| `N` | `T_esc(N)` | `H^1` at the fixed grid time `t = 0.0820928` | `H^1` at the escape event | of which the last shell `4^(N-1) a_(N-1)^2` | `E/E_0`        | log2 slope, `n = 2..8` | log2 slope, `n = 9..N-6` |
| --- | ---------- | -------------------------------------------- | ------------------------- | ------------------------------------------- | -------------- | ---------------------- | ------------------------ |
| 16  | 0.0828279  | 2.0933                                       | 2.1801                    | 0.0000                                      | 1.000000000000 | -1.5373 (7)            | — (window empty)         |
| 20  | 0.0828476  | 2.0933                                       | 2.1839                    | 0.0000                                      | 1.000000000000 | -1.5296 (7)            | -1.5552 (6)              |
| 24  | 0.0828480  | 2.0933                                       | 2.1841                    | 0.0001                                      | 1.000000000000 | -1.5294 (7)            | -1.5159 (10)             |
| 28  | 0.0828480  | 2.0933                                       | 2.2021                    | 0.0180                                      | 1.000000000000 | -1.5294 (7)            | -1.4032 (14)             |
| 32  | 0.0828480  | 2.0933                                       | 6.7961                    | 4.6117                                      | 1.000000000000 | -1.5294 (7)            | -1.2623 (18)             |
| 36  | 0.0828480  | 2.0933                                       | 1182.7783                 | 1180.5916                                   | 1.000000000000 | -1.5294 (7)            | -1.1565 (22)             |

**Neither `H^1` column carries information about the open question of this section**, and the round-1 referee report was right to reject the earlier table, which printed the middle column alone under the heading "`H^1` just before escape". That column is `r["H1"][-2]`, the value at the last point of the `t_eval` grid; that grid is `logspace(-14, log10(t_end), 1500)` and does not depend on `N`, so the number is the state at the *fixed* time `t = 0.0820928`, about `7.6e-4` before the event, and its constancy across `N` is automatic. The event-state column is no better: from `N = 28` on it is dominated by the single last shell, whose contribution `4^(N-1) esc_tol^2` is `0.018, 4.61, 1180.6` at `N = 28, 32, 36` — a pure artifact of the arbitrary threshold `esc_tol = 1e-9`. Both columns are kept, with that reading and no other; the sub-question "is `H^1` finite at `T*`" is untouched by either.

Read: `T_esc(N)` is constant to **six** digits from `N = 24` on, and energy is conserved to `1e-12`, so the escape is a property of the model and not of the truncation — the front reaches `n = infinity` at `T* = 0.082848`. The seventh digit quoted in the earlier version is a threshold artifact: at `N = 32`, `T_esc` moves with `esc_tol` as `0.0830946, 0.0828624, 0.0828480, 0.0828478, 0.0828478` for `esc_tol = 1e-4, 1e-6, 1e-9, 1e-12, 1e-14` (script part 2), so the `esc_tol -> 0` value is `0.0828478` and the digit `...80` was an artifact of the threshold, not of the model.

Relation to the literature, stated as a dependence rather than used as cover (`00_problem.md` section 0). Katz-Pavlovic 2005 is cited for finite-time blowup of an inviscid dyadic model [checked via WebSearch: title, and that they prove blowup for the inviscid case; the paper was not read]. Two *mathematical* identifications would have to hold before that theorem could be said to cover the escape measured above, and **neither was verified anywhere in this project**: (i) that their model is this model with this coefficient base `lam = 2^(5/2)`, which is derived here from Navier-Stokes scaling in C2 and is nowhere claimed to be theirs; and (ii) that their notion of blowup — of some Sobolev norm — is the same event as the threshold crossing at the last retained shell measured here, which is exactly the gap left open by the next paragraph. The earlier version wrote "KNOWN as a theorem" at this point; that tag is **withdrawn**. What is claimed here is COMPUTED and stands on its own, and the citation is a pointer for a reader with library access. The same withdrawal applies to the reference-list description of Kiselev-Zlatos as "the theorem behind C6".

What is **not** established: the shape of the profile at `T*`. The inner range is stable at slope `-1.53`, but the outer-range slope drifts monotonically shallower as the truncation is extended (`-1.56, -1.52, -1.40, -1.26, -1.16` for `N = 20, 24, 28, 32, 36`) and has not converged at `N = 36`. If the asymptotic slope were `-1.53` the solution would still be in `H^1` at `T*` (since `sum 4^n 2^(-3.06 n)` converges); if it is the constant-flux `-5/6` of C5 then `H^1` diverges at `T*`. These runs do not decide it, and the honest label for that sub-question is **FAILED** (attempted, not resolved).

The breaking step, corrected. The earlier version gave it as "the outer window needed to see the asymptote is pushed past `N = 32` by the exponentially small precursor tail of C4, whose slope `-5/2` contaminates the last shells". That is wrong, and by a sign: a `-5/2` contamination biases a least-squares slope *steeper*, while the observed drift is monotonically *shallower*. The profile says what the real obstruction is. Script part 2 prints the per-shell log2 differences of the `N = 32` profile at the event: flat near `-1.55` across the transitions from shell 6 to shell 14, then shallowing monotonically `-1.52, -1.50, -1.45, -1.37, -1.25, -1.09` over the transitions from shell 14 to shell 20, then non-monotone `-0.94, -0.90, -0.80, -0.94, -0.63, -1.25, -0.01, -2.48, +2.46, -7.43, +12.36` over the last eleven transitions — a non-monotone shoulder against the truncation boundary, not a power-law tail. (It is not solver noise: the referee reproduced it to three significant figures for `rtol` from `1e-7` to `1e-12` with `atol` from `1e-14` to `1e-20`.) So the breaking step is **the truncation boundary layer**: shell `N-1` has no drain, the last several shells therefore do not satisfy the equations of the infinite system, and the fitting window `9..N-6` still overlaps that layer at every `N` reached here. That is a harder obstruction than the one previously recorded, and the remedy is the change of variables listed under "What would be needed next", not a larger `N`.

The independent and much sharper evidence for the constant-flux regime is C7 and C8, which measure it in the viscous quasi-steady state where an inertial range genuinely exists. The literature does record a loss of `H^sigma` regularity for inviscid dyadic models at a specific `sigma`, and a search snippet reports "blow-up of the `H^(1/3+delta)` norm" for a generalized dyadic model \[checked via WebSearch: the phrase appears in a snippet about a paper answering a question of Kiselev and Zlatos; the model's normalization is not given in the snippet and I could not reconcile it with the `s`/`alpha` dictionary above, so it is not used\].

### C7. With the true Navier-Stokes dissipation `4^n` the front does NOT escape in any run performed here — **COMPUTED**; the corresponding statement about the untruncated model — **CONJECTURE** (citation-dependent; see "Status of the conclusion" below)

Same data, `s = 1`, integrated to `t = 200`. `n_d` is the shell carrying the peak of the dissipation spectrum `2^(2 s n) a_n^2` at the instant of maximum total dissipation (script part 3).

| `nu` | `A` | `N`     | `T_end`      | max `H^1`  | `n_d`   | `E_end/E_0`       | verdict                                                           |
| ---- | --- | ------- | ------------ | ---------- | ------- | ----------------- | ----------------------------------------------------------------- |
| 0.3  | 1   | 24 / 32 | 200          | 5.33       | 3 / 3   | 1.6e-40 / 4.6e-41 | dissipation wins                                                  |
| 0.3  | 3   | 24 / 32 | 200          | 145.3      | 8 / 8   | 5.6e-42 / 8.8e-42 | dissipation wins                                                  |
| 0.1  | 1   | 24 / 32 | 200          | 16.14      | 8 / 8   | 6.6e-21           | dissipation wins                                                  |
| 0.1  | 3   | 24 / 32 | 200          | 437.2      | 12 / 12 | 7.5e-22           | dissipation wins                                                  |
| 0.03 | 1   | 24 / 32 | 200          | 54.0       | 13 / 13 | 8.8e-10           | dissipation wins                                                  |
| 0.03 | 3   | 24 / 32 | 0.0375 / 200 | 815 / 1459 | 17 / 18 | 0.975 / 9.8e-11   | **truncation artifact at `N = 24`**; dissipation wins at `N = 32` |
| 0.01 | 1   | 24 / 32 | 0.1125 / 200 | 90.6 / 162 | 17 / 18 | 0.975 / 3.2e-7    | **truncation artifact at `N = 24`**; dissipation wins at `N = 32` |
| 0.01 | 3   | 24 / 32 | 0.0292 / 200 | 124 / 4379 | 17 / 22 | 0.999 / 3.5e-8    | **truncation artifact at `N = 24`**; dissipation wins at `N = 32` |

`min_n a_n >= -1e-18` in every run (the largest excursion in the run reproduced above is `-4.12e-19`, roundoff only; the script now prints that minimum instead of the stale bound `-1e-15` it printed before). So positivity is preserved. Whether that places these runs inside the solution class of the Barbato-Morandin-Romito well-posedness theorem is **not verified here** and cannot be, since the paper was not read (`00_problem.md` section 0); the earlier version of this sentence asserted it. The `max H^1` column is a maximum over the 1500-point logarithmic time grid, not over the trajectory; at a stall the dissipation peak is broad and the two agree to the digits printed. The three rows where `N = 24` reports an escape and `N = 32` reports a stall are exactly the truncation false positive one must guard against: the stall shell has moved past the last retained shell. Every verdict in the table is stable under `N = 24 -> 32` once `n_d < N - 6`.

The `(nu, A)` collapse of C3 is verified numerically at `mu = 3`: `(nu, A) = (0.1, 1)` versus `(0.3, 3)` gives `n_d = 8` for both and `max H^1` ratio `8.9998` against the predicted `mu^2 = 9`; likewise `(0.03,1)/(0.09,3)` gives `13/13` and ratio `8.9987`, and `(0.01,1)/(0.03,3)` gives `18/18` and ratio `8.9991`.

The inertial-range slope measured at the instant of peak dissipation, fitted over shells `1..n_d-1`, converges to the constant-flux value of C5 as the inertial range lengthens:

| `nu`  | `n_d` | fitted log2 slope | shells in the fit |
| ----- | ----- | ----------------- | ----------------- |
| 0.03  | 13    | -0.8685           | 12                |
| 0.01  | 18    | -0.8584           | 17                |
| 0.003 | 23    | -0.8500           | 22                |

The reference values are `-5/6 = -0.8333` (the constant-flux profile of C5), `-1/2` (the scale-invariant profile of C2) and `0` (a front cascade, C10). The model selects the constant-flux profile, which is steeper than scale-invariant, and that is why the true dissipation wins.

Status of the conclusion, and the citation dependence, stated at the point of use (`00_problem.md` section 0). What is established here is **COMPUTED and about truncated systems**: in every `(nu, A, N)` combination above, with `s = 1`, the excitation stalls at an interior shell and the energy decays by twenty to forty orders of magnitude. The stronger statement — "the dyadic model with the true Navier-Stokes dissipation is globally regular" — is a statement about the untruncated system, and this line does **not** prove it and cannot get it from these runs (see C9). The earlier version of this section was headed "reproduces a KNOWN theorem" and rested that tag on two citations. Neither can carry it:

- Barbato-Morandin-Romito 2011 prove well-posedness of positive solutions of the viscous dyadic problem "in the relevant scaling range which corresponds to Navier-Stokes" [checked via WebSearch: title, venue Nonlinearity 24 (2011) 3083-3097, and this phrase from an abstract snippet]. **The precise interval of exponents, the definition of the solution class, and the coefficient normalization are all unverified**, since the paper was not read. A theorem whose hypotheses are unknown cannot certify that `s = 1` is inside its range.
- Cheskidov 2008 is reported to prove global regularity for `alpha >= 1/2` and blowup for `alpha < 1/3`, with local regularity for `alpha > 1/3` [checked via WebSearch: arXiv math/0601074, TAMS 2008, and a snippet stating these thresholds]. Under this file's own **inferred** dictionary `alpha = 2 s / 5` ("Setting and notation"; inferred, not read off the paper), `alpha >= 1/2` is `s >= 5/4` and `alpha > 1/3` is `s > 5/6`. **So the global-regularity half of that result does not reach `s = 1` at all**, and the earlier claim that C7 reproduces it at `s = 1` was wrong on the file's own arithmetic.

Accordingly: the table above is COMPUTED; "the untruncated dyadic model with `s = 1` is globally regular" is **CONJECTURE** here, consistent with everything measured and with what the two papers are reported to say, and it would become KNOWN only for a reader who checks Barbato-Morandin-Romito's exponent range and solution class against this normalization. What this line adds that does not depend on any citation is the quantitative law C8.

### C8. The stall shell obeys `n_d = log2(1/nu)/(2s - 5/3) + const`; the divergence of that law is consistent with the predicted threshold `5/6` at the few-percent level — **COMPUTED**

Prediction (CONJECTURE: it is the C10 dichotomy under the regime hypothesis, applied to the constant-flux profile of C5; the exponent arithmetic inside it is the PROVED-HERE lemma of C10). In the constant-flux state the nonlinear turnover rate at shell `n` is `lam^n a_n ~ (eps)^(1/3) 2^(5n/3)` while the damping rate is `nu 2^(2 s n)`, so the cascade stalls at `n_d` with `2^(5 n_d/3) ~ nu 2^(2 s n_d)`, i.e. `n_d = log2(eps^(1/3)/nu)/(2 s - 5/3)`, which diverges as `s -> 5/6` from above and is negative (no stall: the front escapes) for `s < 5/6`. What the scans below test is this predicted law; the law is the object being measured, not something the measurements presuppose.

Scan 4a, `nu(s) = 2^(-14 max(2s - 5/3, 2/15))` chosen so that the predicted stall lands mid-range, `N = 24` and `N = 32` as a truncation control. `T_esc` is the time at which the last retained shell is reached (`inf` when it never is):

| `s`    | `nu`     | `n_d` (24) | `n_d` (32) | `T_esc` (24) | `T_esc` (32) | verdict                                                            |
| ------ | -------- | ---------- | ---------- | ------------ | ------------ | ------------------------------------------------------------------ |
| 0.700  | 0.274    | 0          | 0          | 0.08706      | 0.08726      | escapes at both `N`; `T_esc` drift 24->32 = +0.23% (but see below) |
| 0.800  | 0.274    | 0          | 0          | 0.09447      | 0.10051      | escapes at both `N`; `T_esc` drift 24->32 = +6.39%                 |
| 0.8333 | 0.274    | 0          | 1          | 0.10883      | inf          | borderline (escape at one `N` only)                                |
| 0.900  | 0.274    | 2          | 2          | inf          | inf          | dissipation wins (no escape at either `N`)                         |
| 1.000  | 0.0394   | 12         | 12         | inf          | inf          | dissipation wins (no escape at either `N`)                         |
| 1.100  | 0.00565  | 14         | 14         | inf          | inf          | dissipation wins (no escape at either `N`)                         |
| 1.250  | 0.000308 | 15         | 15         | inf          | inf          | dissipation wins (no escape at either `N`)                         |
| 1.350  | 4.42e-05 | 15         | 15         | inf          | inf          | dissipation wins (no escape at either `N`)                         |

The `T_esc` columns matter, and they are the reason this table alone is not the threshold evidence — but not in the way the earlier version claimed. That version said "at `s = 0.7` the escape time is truncation-independent, as it is for the inviscid model in C6, so the front genuinely reaches infinity", and rejected `s = 0.8` for a monotone drift. Script part 4c now runs both sweeps and prints them, and the distinction does not survive:

```text
s = 0.70, N = 18,22,26,30,34:  T_esc = 0.086957, 0.087028, 0.087079, 0.087165, 0.087436   (monotone, +0.55%, still rising)
s = 0.80, N = 18,22,26,30,34:  T_esc = 0.091376, 0.093401, 0.095562, 0.098240, 0.104851   (monotone, +14.75%, still rising)
```

Both drift monotonically upward and both are still rising at `N = 34`; they differ in rate, not in kind, and neither looks like the inviscid model of C6, where `T_esc` is fixed to six digits from `N = 24` on and does not move at `N = 36`. What separated the two rows in the earlier table was a hard-coded 1% convergence test inside `part4_threshold`, which is arbitrary; that test has been removed and the verdict column now prints the measured drift instead. **Consequence: the sub-threshold side of this table contains no truncation-independent escape at all**, so the boolean "escaped" is evidence of nothing here, and the whole threshold argument rests on the slope law 4b below. (The `s = 0.8` sequence quoted in the earlier version — `0.0895, 0.0917, 0.0941, 0.0964, 0.1040` — was not printed by any script and is not reproducible; the sequence above is what the script prints and replaces it.)

The transition in the boolean verdict does sit near the predicted `5/6 = 0.8333`, and the numerics become `N`-dependent on approach to it from both sides, which is what a diverging stall shell must look like. Two caveats belong here. First, by the previous paragraph, the escapes on the low side are not demonstrated escapes. Second, the `n_d` column of this scan does not itself illustrate the law it is presented alongside: `nu(s)` is chosen so that `log2(1/nu)/(2s - 5/3) = 14` identically in every row, yet the observed `n_d` is `12, 14, 15, 15` at `s = 1.00, 1.10, 1.25, 1.35` but `2` at `s = 0.90` and `0` or `1` at `s = 0.8333`. That is expected — the additive constant in `n_d = log2(1/nu)/(2s-5/3) + const` hides `log2(eps^(1/3))/(2s - 5/3)`, and the flux `eps` collapses as the threshold is approached, so the constant swings by about a dozen shells there — but it was not said before, and it means the mid-range placement claimed for this scan holds only well above the threshold. The 4b fit is unaffected, since the constant cancels at fixed `s`.

Scan 4b makes it quantitative: for each `s`, four values of `nu` spanning up to six decades, `N = 34`, least-squares slope of `n_d` against `log2(1/nu)`:

| `s`  | `nu` range    | `n_d` values   | fitted slope | predicted `1/(2s-5/3)` |
| ---- | ------------- | -------------- | ------------ | ---------------------- |
| 1.00 | 0.3 .. 0.0024 | 3, 10, 17, 24  | 3.015        | 3.000                  |
| 1.10 | 0.05 .. 5e-5  | 8, 14, 20, 27  | 1.896        | 1.875                  |
| 1.25 | 0.01 .. 1e-7  | 9, 15, 22, 29  | 1.207        | 1.200                  |
| 1.40 | 1e-3 .. 1e-9  | 10, 15, 21, 27 | 0.858        | 0.882                  |

Regressing the reciprocal of the fitted slope on `s` gives `1/slope = 2.0803 s - 1.7571` against the predicted `2 s - 5/3`, and hence a numerically located threshold

```text
s* = 0.8446      (predicted 5/6 = 0.8333, error +1.4%; leave-one-out range 0.8338 .. 0.8550)
```

The precision of that number, honestly stated (the earlier version's "locates the blowup threshold to about one percent" is withdrawn). The fitted slope is `+4.0%` off the predicted `2` and the intercept `+5.4%` off `-5/3`; the root lands `1.4%` from `5/6` only because those two errors partly cancel. The fit uses four points, all at `s` in `[1.00, 1.40]`, and `s* = 0.8446` is an extrapolation well below that interval. The dependent variable `n_d` is an integer (`argmax` over shells of the dissipation spectrum), so each of the sixteen underlying measurements carries at least half a shell of quantization, and no error model is propagated. A leave-one-out refit over the four `s` values gives `s*` in `0.8338 .. 0.8550` (script part 4b), which brackets `5/6 = 0.8333` at its lower end. **The defensible statement is: the divergence point of the measured stall law is consistent with `5/6` at the few-percent level.** That is still the checkable object of this line — a one-parameter family of measurements with a predicted divergence, rather than a binary verdict that a truncation could fake — but it is a few-percent consistency, not a one-percent determination.

### C9. The hard self-test against Lions' theorem — **passes**

The self-test required by the task is: if the model blew up at `s >= 5/4` it would have broken the abstract structure it is supposed to share with Navier-Stokes, since `00_problem.md` item 2.10 records Lions' theorem as proving regularity for `s >= 5/4`. (The earlier version added that Lions' proof, "being an energy-plus-interpolation argument, covers averaged and dyadic analogues". That sentence is withdrawn: `00_problem.md` tags Lions 1969 as \[checked via WebSearch: book and the `alpha >= 5/4` attribution\] only, so any description of the internal structure of that proof is a statement about an unread text, and in any case a dyadic ODE system is not an instance of the theorem — the estimate would have to be redone. What makes `5/4` the right comparison number here is internal: the power-law lemma of C10 gives `5/4` as the maximal-cascade threshold of *this* model, by arithmetic performed in this file.) Measured: at `s = 1.25` the model stalls at a finite shell for every `nu` tested down to `1e-7`, at `s = 1.40` down to `1e-9`, and at both the stall shell moves with `log2(1/nu)` along the same law (C8); the divergence point of that law is at `s* = 0.8446`, far below `5/4`. So the test passes, and with room to spare.

What that does **not** license, and what the earlier version claimed: "the model is regular not only in the subcritical range `s >= 5/4` but throughout `s > 5/6`". Every system integrated in this line is a truncated, finite-dimensional ODE with the exact energy identity of C1, hence `|a(t)| <= sqrt(E_0)` for all `t` and global existence is automatic; no truncated run can exhibit or exclude blowup of the infinite system. The only observable is whether the excitation reaches the last retained shell before `t = 200`, and this file itself identifies that observable as a false positive in three rows of C7 and, by the corrected part 4c above, in the `s = 0.7` and `s = 0.8` rows of C8. The stall evidence (an interior dissipation peak with `n_d < N - 6`, energy down by twenty to forty orders of magnitude, verdicts stable under `N = 24 -> 32`) is good evidence and each row's verdict stands as COMPUTED; but "the untruncated model is regular throughout `s > 5/6`" is **CONJECTURE**. The interesting content survives the downgrade: the conjectured regularity window `(5/6, 5/4)` is genuinely *supercritical*, a range in which Navier-Stokes itself is open, and C10-C11 identify the structural property — relaxation to the constant-flux profile — that would explain it.

### C10. Which cascades beat dissipation `2^(2 s n)`: a power-law comparison lemma — **PROVED-HERE** — and the regime dichotomy built on it — **CONJECTURE**

The round-1 referee report was right that the version of this section tagged PROVED-HERE was scaling heuristics: it assumed the regime it characterized, used undefined `>>` and `~`, and its "Fact 1" was not a theorem. The section is therefore split. What is arithmetic is isolated as a lemma and keeps PROVED-HERE; the dichotomy that applies it to "the cascade" is **CONJECTURE**, and everything downstream that says "by C10" inherits that status — including the `5/6` and `5/4` thresholds, the `(d+2)/4` dimension corollary, the prediction behind the C8 stall law, and both breakpoints.

**Lemma (power-law comparison) — PROVED-HERE.** Let `lam = 2^L` with `L > 0`, let `nu > 0`, `s > 0`, `c > 0`, and let `A(n) = c 2^(-b n)`. Then, exactly,

```text
(i)   sum_{n>=0} 1/(lam^n A(n))  =  (1/c) sum_{n>=0} 2^((b - L) n)        converges  iff  b < L,
(ii)  lam^n A(n) / (nu 2^(2 s n))  =  (c/nu) 2^((L - b - 2 s) n)          -> +infinity  iff  b < L - 2 s,
                                                                          -> 0          iff  b > L - 2 s.
```

Proof. Both displayed identities are immediate substitutions, and each right-hand side is a geometric series (resp. sequence) with ratio `2^(b-L)` (resp. `2^(L-b-2s)`); a geometric series converges iff its ratio is `< 1` and a geometric sequence diverges iff its ratio is `> 1`. QED. (Script part 1h prints both thresholds with `L = 1 + d/2` symbolic in `d` and evaluates them on the two profiles below.)

**Corollary of the lemma — PROVED-HERE.** At `d = 3`, `L = 5/2`: for the constant-flux profile `b = 5/6` (C5), the ratio in (ii) diverges iff `s < 5/6`; for a non-decaying front `b = 0`, iff `s < 5/4`; and both satisfy (i), since `5/6 < 5/2` and `0 < 5/2`. These two numbers are the `5/6` and the `5/4` quoted throughout this file. Note what the corollary is: a relation between a decay exponent and a dissipation exponent. It says nothing by itself about whether any solution of the model has front amplitude `c 2^(-b n)`.

**The dichotomy — CONJECTURE.** Setup, with the hypothesis marked. Call `A(n)` the amplitude of shell `n` at the moment `T_n` at which it is the active (energy-carrying) shell of the cascade, and **assume** — this is the regime hypothesis, and it is assumed, not derived — that the cascade advances one shell per turnover, so that `T_{n+1} - T_n ~ 1/(lam^n A(n))`, where `~` means "equal up to a factor bounded above and below by constants independent of `n`" (the coefficient of the transfer term out of shell `n` is `lam^(n+1)` and the partner amplitude is taken comparable to `A(n)`). Under that hypothesis:

Three items follow under that hypothesis. The arithmetic inside each is the lemma above; the modelling is the hypothesis, and it is where the status comes from.

1. **Finite escape time.** The cascade reaches `n = infinity` at the finite time `T* = T_0 + sum_n (T_{n+1} - T_n)` iff `sum_n 1/(lam^n A(n)) < infinity`; for a power-law `A` this is part (i) of the lemma, i.e. `b < 5/2`. Both regimes considered below satisfy it comfortably. *Withdrawn:* the earlier version of this item read "`sum_n 1/(lam^n A(n)) < infinity` iff `A(n)` decays slower than `lam^(-n)` up to summable corrections". That is not a theorem — the qualifier carries the content — and its "if" direction is false: `A(n) = lam^(-n) log(n+2)` decays strictly slower than `lam^(-n)` (the ratio tends to infinity) yet `sum_n 1/(lam^n A(n)) = sum_n 1/log(n+2)` diverges. Only the converse is elementary: if the sum converges then `lam^n A(n) -> infinity`. Part (i) of the lemma is what is used below.
2. **Dissipation is beaten at shell `n`** when the transfer rate exceeds the damping rate, `lam^n A(n) >> nu 2^(2 s n)`, with `>>` read precisely as "the ratio tends to `+infinity` as `n -> infinity`". For a power-law `A` this is part (ii) of the lemma: `b < 5/2 - 2 s`, i.e. `A(n) 2^((5/2 - 2s) n) -> infinity`. At `s = 1` the threshold exponent is `b < 1/2`: the front amplitude must decay **strictly slower than the scale-invariant profile of C2**.
3. **Energy bounds the front** (this one is exact, PROVED-HERE, from C1 with `nu >= 0`). `A(n)^2 <= sum_m a_m^2 = E(t) <= E_0`, so `A(n) <= sqrt(E_0)`: the best possible cascade in this class is one in which an order-one fraction of the total energy occupies the single active shell, i.e. `b = 0`.

Consequences (CONJECTURE, inherited through the regime hypothesis). Substituting the constant-flux profile `b = 5/6` of C5 into item 2 gives `s < 5/6`: the constant-flux cascade beats dissipation exactly below the threshold whose measured analogue is C8. Substituting the maximal profile `b = 0` (item 3) gives `s < 5/4`.

What is proved and what is not. Proved: the lemma and its corollary (exponent arithmetic), and item 3 (the energy bound). Not proved, and each is a statement about the ODE system rather than about exponents: (a) that a cascade advances one shell per turnover with `T_{n+1} - T_n ~ 1/(lam^n A(n))`; (b) that the front amplitude follows a power law at all — the inviscid profile of C6 is demonstrably not a clean power law; (c) that "the transfer rate exceeds the damping rate at every shell" implies the front actually reaches `n = infinity`. The empirical input that makes the picture worth stating is separate and is COMPUTED: the measured inertial-range exponents (C7: `-0.8685, -0.8584, -0.8500`; C11: six chain models between `-0.8975` and `-0.8590`) sit at the constant-flux value, and the measured stall law (C8) has the divergence point the corollary predicts for that exponent, to within a few percent.

Two readings, and both matter.

- The engineered ("front", Tao) regime is the one in which `A(n)` does not decay at all (`b = 0`) — an order-one fraction of the energy is handed from one scale to the next and the shells behind are left empty. This is the picture of Tao's "self-replicating machine": each stage builds a rescaled copy of itself at the next scale and transfers essentially all of its energy into it \[the description of that construction is from `00_problem.md` item 2.2 and memory of the accompanying blog post — unverified, the paper was not read\]. Under the regime hypothesis its threshold is `s = 5/4`, i.e. the maximal cascade of this type would stop exactly at the exponent that `00_problem.md` item 2.10 attributes to Lions. That coincidence is a consistency check on the picture, and the two `5/4`s do have a common source in this file's own arithmetic (energy criticality in C2, and `b = 0` in the corollary above) — but it is not a theorem that no cascade mechanism can contradict item 2.10, and the earlier version of this bullet asserted it as one.
- The dyadic model does not realize the front regime; it relaxes to constant flux (measured in C7: `-0.8685, -0.8584, -0.8500 -> -5/6`), losing the factor `2^(-n/3)` between `2^(-5n/6)` and `2^(-n/2)` per shell. That single lost factor is the entire gap between `s* = 5/6` and the true dissipation `s = 1`.

**Corollary (dimension check).** The arithmetic is PROVED-HERE; the reading of it is CONJECTURE, through the regime hypothesis. Redo C2 in dimension `d`: the map `u -> lam0 u(lam0 x, lam0^2 t)` at `lam0 = 2` multiplies a shell amplitude by `2^(1-d/2)` (since `||u_lam0||_{L^2} = lam0^(1-d/2) ||u||_{L^2}`) and time by `1/4`, so `sig = 1/4`, `mu = 2^(d/2-1)` and `lam = mu/sig = 2^(1+d/2)`, i.e. `L = 1 + d/2`; at `d = 3` this is `2^(5/2)`, at `d = 2` it is `4`, at `d = 1` it is `2^(3/2)`. The lemma with `b = 0` then gives the front-regime threshold `s < (d+2)/4` (`5/4` at `d = 3`, `1` at `d = 2`, `3/4` at `d = 1`, `3/2` at `d = 4`), and with the `d`-dimensional constant-flux exponent `b = (d+2)/6` — from `L = 3b`, the exponent that makes `lam^(n+1) a_n^2 a_{n+1}` independent of `n` — the realized-regime threshold `s < (d+2)/6` (`5/6` at `d = 3`, `2/3` at `d = 2`, `1/2` at `d = 1`). Script part 1h prints all six numbers.

Reading, corrected. In the **realized** (constant-flux) regime the mechanism degrades as the dimension drops exactly as transfer test T2 requires: at `d = 2` its threshold is `2/3 < 1`, so the true dissipation defeats it, consistent with the global regularity of 2D Navier-Stokes (no-go item 2.8); at `d = 1` it is `1/2 < 1`, consistent with the regularity of viscous Burgers (the T2 comparison list). In the **maximal** (front) regime at `d = 2`, however, the threshold is `(d+2)/4 = 1`, which is exactly the true dissipation: the comparison is *marginal*, and a scaling comparison at marginality decides nothing. So the honest verdict for the maximal regime at `d = 2` is **inconclusive**, not "T2 passing"; the earlier version's closing sentences ("transfer test T2 passing in a quantitative form ... it saturates rather than violates the hyperdissipation barrier in every dimension") overstated this and are withdrawn. What survives is: the realized regime degrades in the right direction in every dimension checked, and the maximal regime degrades in the right direction for `d = 1` and is marginal at `d = 2`.

### C11. Why a one-mode-per-scale cascade cannot reach the front regime, and why adding modes in a chain does not help — **CONJECTURE**, with **COMPUTED** support

Consider any *chain* model `da_j/dt = -nu kappa_j a_j + c_j a_{j-1}^2 - c_{j+1} a_j a_{j+1}` with arbitrary positive coefficients `c_j`, `m` modes per dyadic scale, `kappa_j = 2^(2 s n(j))` constant across the `m` modes of scale `n(j)`, and scale invariance `c_{j+m} = lam c_j`. Every such chain has the exact energy identity of C1 (script part 5a verifies that the trilinear form vanishes identically in sympy with all `c_j` symbolic), so all of them are admissible carriers as far as the energy identity is concerned.

Heuristic obstruction (the reason the conjecture is stated, not proved), with its own gap marked. For a *complete* transfer, mode `j` should fill before it starts to drain, i.e. its outflow rate `c_{j+1} a_j a_{j+1}` should be small compared with its inflow rate `c_j a_{j-1}^2` during filling. The earlier version turned that into "informally `c_{j+1} << c_j`" and then ran the following argument: iterating over the `m` modes of one scale multiplies the coefficient down by a factor `< 1`, while scale invariance forces the product of the `m` ratios across a scale to be exactly `lam = 2^(5/2) > 1`, so at least one step within each scale must have `c_{j+1} >= c_j`, and at that step the next mode drains faster than the current one fills. **The step from the rate comparison to `c_{j+1} << c_j` is a non sequitur**, as the round-1 referee pointed out: it drops the amplitudes, and during filling `a_{j+1}` is precisely the small quantity, so a large `c_{j+1}` can be compensated by a small `a_{j+1}` and the conclusion about the coefficients alone does not follow. The coefficient bookkeeping (the product of the `m` intra-scale ratios is `lam`, hence at least one ratio is `>= lam^(1/m) > 1`) is correct and is PROVED-HERE arithmetic; what is conjectural is that this forces the chain to constant flux. That is why C11 is CONJECTURE, and the numerics below are its only real support.

Numerical support (script part 5b, `s = 1`, 26 scales, `nu` chosen per pattern so the stall lands inside the truncation): the per-scale log2 amplitude slope in the inertial range, for one, two and three modes per scale and for strongly asymmetric intra-scale coefficient patterns, stays at the constant-flux value and never approaches the front value `0`:

| `m` | intra-scale pattern `C_i` | `nu` | stall scale `n_d` | log2 slope per scale | scales in the fit |
| --- | ------------------------- | ---- | ----------------- | -------------------- | ----------------- |
| 1   | (1)                       | 3e-3 | 19                | -0.8975              | 18                |
| 2   | (1, 1)                    | 3e-3 | 13                | -0.8693              | 12                |
| 2   | (8, 1/8)                  | 3e-6 | 15                | -0.8735              | 14                |
| 2   | (1/8, 8)                  | 1e-4 | 15                | -0.8625              | 14                |
| 3   | (1, 1, 1)                 | 3e-3 | 12                | -0.8688              | 11                |
| 3   | (4, 1, 1/4)               | 1e-4 | 17                | -0.8590              | 16                |

(`nu` is chosen per pattern so that the stall lands inside the 26-scale truncation; the intra-scale pattern shifts the stall scale by up to a dozen scales, which is the `O(1)` offset of the C8 law, and leaves the slope alone.)

Interpretation: "several modes per scale" is necessary but nowhere near sufficient. What Tao's construction adds beyond more modes is a genuine three-mode interaction that acts as a *gate* — a mode whose amplitude switches a transfer on only after the previous transfer has completed — which cannot be produced by a chain of pairwise transfers, because a pairwise transfer `u' = -c u v`, `v' = c u^2` starts as soon as `u != 0` and its partner is nonzero. Recording the full gadget (pump, rotor, transfer gadget with a delayed switch, exact energy conservation, and a renormalization step exhibiting geometrically decreasing transfer times) as CONJECTURE with this proof plan is the honest outcome; it was not built here.

### C12. What blocks porting the mechanism to true Navier-Stokes — **KNOWN** for the cited formulas and no-go items only; **CONJECTURE** for the inferences drawn from them in points 1-4

The mechanism of C10 needs a nonlinearity whose triad coefficients can be chosen: to build a gate one must be able to switch a particular interaction off while leaving another on. The true nonlinearity does not allow this, for four separate reasons, each of which is also the reason one of the transfer tests bites.

1. **The triad coefficients are fixed by geometry, not choosable** [the formula is KNOWN and standard; the inference drawn from it is mine, CONJECTURE]. For `B(u,v) = -P((u.grad)v)` on `T^3`, the interaction coefficient of the triad `k = p + q` is determined by the Fourier coefficients through `-i P_k (u_p . q) v_q`, with `P_k = I - k k^T/|k|^2` the Leray projector — a single fixed formula with no free parameters. Tao's averaging (item 2.2) replaces `B` by an average of rotated and multiplier-modified copies of `B`, which is reported to change those coefficients while preserving the energy identity `<B~(u,u),u> = 0`, the scaling, and every bilinear estimate [checked via WebSearch only at the level of the paper's existence and headline statement; the construction was not read]. The earlier version continued "that freedom is exactly what makes the gadget constructible for `B~` and not for `B`". That sentence asserts an impossibility for `B`, which is the open question this line is about, and it is withdrawn as an assertion and kept as **CONJECTURE**. This is transfer test T1 in its sharpest form: any argument that would show the mechanism of C10 fails for `B` must use something that averaging destroys.
2. **Transport structure and the vorticity equation.** The exact nonlinearity is a transport term plus a stretching term: `(d_t + u.grad) omega = (omega.grad) u + nu Laplacian omega`. Transport gives Lagrangian conservation laws (Kelvin circulation, helicity in the inviscid case), a maximum principle in two dimensions and in the swirl-free axisymmetric case (no-go items 2.8, 2.9), and the BKM criterion `int_0^{T*} ||omega(t)||_infinity dt = infinity` (item 2.7). A dyadic or averaged nonlinearity has none of this; in particular the dyadic model has no vorticity, no BKM criterion, and no notion of the direction of vorticity (Constantin-Fefferman). Transfer test T2 says a blowup mechanism that survives the 2D and swirl-free reductions is wrong, and the cascade of C10 has no way of even being restricted to them — it is not a mechanism *for* Navier-Stokes until it is realized by an actual velocity field.
3. **The pressure is nonlocal.** `p = sum_{i,j} R_i R_j (u_i u_j)` with `R_i` the Riesz transforms, so the projector `P` couples every mode to every other; one cannot localize a gadget in frequency space and expect it to stay decoupled from the rest of the flow, and one cannot add a "gate mode" without it interacting with everything through the pressure. This is also why the Burgers comparison in T2 has force: without pressure the equation is regular by the maximum principle, so the mechanism must use incompressibility, and incompressibility is precisely what makes the interactions unchoosable.
4. **The scaling budget is already saturated** [CONJECTURE: this inherits C10's regime hypothesis]. By C10 the maximal cascade in this class beats dissipation only for `s < 5/4`; at `s = 1` it needs the front amplitude not to decay. For real Navier-Stokes the analogous statement would be that a blowup must keep an order-one fraction of the energy in the active scale at every stage of the cascade, which by `00_problem.md` 1.6 would mean the solution stays of order one in a critical norm all the way down — consistent with, and no stronger than, the ESS constraint `lim ||u(t)||_{L^3} = infinity` of item 2.4 and Leray's lower bounds of item 2.5. In other words the cascade picture does not by itself contradict any known constraint; it also does not by itself produce a velocity field. The transfer of the model statement to Navier-Stokes is an analogy, not an implication.

**The fluid computer.** Cardona, Miranda, Peralta-Salas and Presas, "Constructing Turing complete Euler flows in dimension 3", PNAS 118 (2021) [checked via WebSearch: authors, title, venue, and the statement that they build stationary Euler flows on a Riemannian 3-manifold that simulate a universal Turing machine, with undecidable trajectories], and Dyhr, Gonzalez-Prieto, Miranda and Peralta-Salas, "Turing complete Navier-Stokes steady states via cosymplectic geometry", arXiv:2507.07696 (2025), PNAS Nexus [checked via WebSearch: authors, title, arXiv id, venue, and the statement that universality holds on manifolds admitting a nonvanishing harmonic 1-form, so viscosity does not obstruct universality], show that fluid dynamics can carry universal computation. It is tempting to read this as "one can program a fluid to blow up". That inference fails, for a reason worth stating precisely: **the constructed flows are stationary.** A steady solution `u(x)` has `||u(t)||_{H^s} = ||u(0)||_{H^s}` for all `t`, so it never blows up; the computation is carried by the *trajectories of the fluid particles*, i.e. by the flow of a fixed vector field, not by the time evolution of the velocity field itself. The undecidability is about where a particle goes, not about whether the solution stays smooth. To convert Turing completeness into a blowup one would need a *non-stationary* computation whose steps are the transfers of C10, running at a geometrically accelerating clock rate set by the local amplitude, with each step's gadget occupying half the linear scale of the previous one — none of which the stationary constructions provide, and the accelerating clock is exactly the part that the fixed triad coefficients of point 1 obstruct. The argument rests on stationarity alone, and on nothing about the underlying manifold; the same group also constructed Turing-complete stationary Euler flows of Beltrami type in Euclidean space ("Computability and Beltrami fields in Euclidean space", arXiv:2111.03559 [checked via WebSearch: title only]).

Exclusion from the Clay class, which the earlier version declined to state — **PROVED-HERE**, and it needs no reading of either paper. In the Clay setting of `00_problem.md` 1.1-1.3 the force is `f` with the decay hypothesis (5) (whole space) or (10) (torus). For an unforced steady solution on `T^3`, pairing (1) with `u` and integrating gives `0 = -nu int |grad u|^2`, so `grad u = 0` and `u` is constant: a steady unforced Clay solution carries no computation at all. A nonzero *time-independent* force does not satisfy (10), which requires decay in `t` faster than every power. So a stationary Turing-complete flow is either forceless — hence constant — or driven by a force outside the Clay class; either way it is not a counterexample to (A)-(D), and no geometric condition is needed to see it. The flat-torus remark that the earlier version leaned on is in any case vacuous: the harmonic 1-forms on the flat torus are exactly the constant ones. Tagging: the statements about the two papers are [checked via WebSearch] as indicated; the exclusion just given is PROVED-HERE; the reading of what those papers do and do not imply for blowup is CONJECTURE (mine, not claimed by those authors).

## Breakpoint

This line stops at two places, and it is worth separating them because the second is much harder than the first.

**Breakpoint 1 (inside the ODE world).** C10 conjectures — as scaling heuristics around a proved power-law lemma — that a cascade beats the true dissipation exactly when its front amplitude `A(n)` decays strictly slower than `2^(-n/2)`, and C11 conjectures that no chain of pairwise transfers achieves that. The reason offered in C11 is a rate comparison ("each transfer must complete before the next begins") plus the coefficient bookkeeping that scale invariance forces the product of the intra-scale coefficient ratios to be `lam` per scale; the step from the rate comparison to a statement about the coefficients alone is a non sequitur and is marked as such in C11, so what is left is six chain models that all land at the constant-flux exponent. The exact step where this line stops: **a gate has to be built out of a genuine three-mode interaction, and I did not build one.** Concretely, one needs modes `(x, y, g)` with an energy-conserving quadratic law (`x' = a y g`, `y' = b x g`, `g' = c x y`, `a + b + c = 0`) arranged so that the `x -> y` transfer is off until `g` has grown, with the growth of `g` timed to the completion of the previous scale's transfer, and the whole triple reproducing itself at the next scale under the scaling map of C2 with time contracted by `1/4`. Tao's construction is reported to do this with several modes per scale (a pump, a rotor and a transfer gadget) and a careful choice of amplification factors [unverified: the paper was not read]. C11 is therefore CONJECTURE, not a theorem: a mechanism for why chains fail, numerics showing six of them failing in the same way, no proof that no chain works, and no explicit gadget that does.

**Breakpoint 2 (the port to the true equation, and the real one).** Even a completed gadget is an ODE system, not a fluid. To port it one must exhibit divergence-free vector fields `psi_{n,i}` on `T^3`, with `psi_{n,i}` localized at frequency `~2^n`, such that the *actual* Euler trilinear coefficients `<B(psi_{n,i}, psi_{n,j}), psi_{n',k}>` — with `B(u,v) = -P((u.grad)v)` and `P` the Leray projector — equal the gadget's coefficients up to errors the construction can absorb. **This is where the line stops: those coefficients are not free parameters.** For the triad `k = p + q` on the torus they are fixed by `-i P_k (u_p . q) v_q` with `P_k = I - k k^T/|k|^2`; there is nothing to tune, and the projector couples every mode to every other (the pressure is nonlocal). The earlier version added "and a gate needs a specific interaction to be *absent*, which the geometry does not permit". **That is false as stated and is withdrawn**: individual coefficients do vanish, by at least two elementary mechanisms — `P_k` annihilates any vector parallel to `k`, so the coefficient vanishes whenever the resulting vector is longitudinal, and the scalar factor `u_p . q` vanishes whenever `u_p` is orthogonal to `q` — so specific interactions certainly can be switched off. What is true, and all that is claimed here, is that the coefficients cannot be prescribed *as a family*: one is choosing a finite set of divergence-free wave packets, not a table of numbers, and every wanted coefficient comes with all the unwanted ones the same packets generate. Whether a gate can nonetheless be assembled from packets whose unwanted coefficients are small enough is **open**, and this line does not settle it; that is exactly the measurement proposed in "What would be needed next". Tao's resolution is not to port the gadget but to change the nonlinearity: `B~`, an average of rotated and multiplier-modified copies of `B`, is reported to have choosable coefficients while keeping the energy identity, the scaling and every bilinear estimate (no-go item 2.2) [unverified: not read]. So the honest statement of the breakpoint is: **this line reproduces the reason the averaged equation is said to blow up, and the reason the same construction cannot be written down directly for the exact equation, and it does not narrow the gap between them by one line.** Closing the gap in the blowup direction requires a genuinely new idea about how to realize a delayed, amplitude-amplifying transfer with the coefficients that the Leray projector actually gives; closing it in the regularity direction requires (by transfer test T1) an argument that uses vorticity transport, the Lagrangian structure, the Biot-Savart geometry, the pressure formula or a maximum principle, none of which the models here possess at all.

A third, smaller gap is recorded honestly in C6: the shape of the inviscid profile at the escape time is not resolved by these runs, so whether `H^1` is finite at the model's inviscid blowup time is left open here.

## Transfer check

Rows are the no-go items and transfer tests of `00_problem.md`. "Mechanism" below means the cascade of C10; "the model" means the dyadic system of C1-C8.

| item                                         | verdict                                                                                                                                        | reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2.1 supercriticality barrier                 | passes                                                                                                                                         | The stalls measured at `s > 5/6` (C7, C8) are not obtained from the energy identity and scaling alone: the runs use positivity and the one-way flux structure of C5, and the model **selects** the constant-flux profile in every run performed here. That the profile is *forced* is not proved anywhere in this line — it is the open problem listed first under "What would be needed next" — so this row is COMPUTED, not an argument. Navier-Stokes has neither positivity nor a one-way flux, so nothing here transfers to a regularity proof for (A)/(B).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2.2 averaged Navier-Stokes blowup            | target                                                                                                                                         | This line is a reconstruction of the mechanism half of item 2.2 on ODE models. C10 gives the amplitude criterion the machine must satisfy; C11 records that the machine itself was not built.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2.3 no backward self-similar solutions       | passes                                                                                                                                         | C4 exhibits an exact self-similar blowup of the model with infinite energy, so a finite-energy blowup of the model must be a front. As C4 now records, that is a *reading* parallel to no-go item 2.3 (Necas-Ruzicka-Sverak, Tsai) and not an instance of it: their exclusions concern small-scale behavior and `L^3`, the divergence here is at large scales, and the model has neither a velocity field nor an `L^3` norm. Consistent with 2.3; not a substitute for it, and no inference is drawn from one to the other.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2.4 critical `L^infinity_t L^3_x` (ESS)      | not applicable                                                                                                                                 | The model has no velocity field and no `L^3` norm. C12 point 4 notes that the front regime is consistent with, and weaker than, the ESS constraint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2.5 LPS conditions, Leray lower bounds       | not applicable                                                                                                                                 | Same reason; no space variable, no `L^q_t L^p_x`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2.6 partial regularity (CKN)                 | not applicable                                                                                                                                 | No space-time singular set in a shell model.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2.7 Beale-Kato-Majda                         | not applicable, and diagnostic                                                                                                                 | The model has no vorticity, so no BKM criterion; C12 point 2 uses precisely this absence as one of the four obstructions to porting.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2.8 2D global regularity                     | not applicable as a faithful comparison; scaling arithmetic partly inconclusive                                                                | A shell model has no scalar vorticity and no enstrophy conservation, so there is no faithful 2D comparison system here. What can be said is scaling arithmetic (C10 corollary, CONJECTURE through the regime hypothesis): at `d = 2` the realized constant-flux regime has threshold `2/3 < 1`, so the true dissipation defeats it — the mechanism weakens in the right direction. But the maximal (front) regime at `d = 2` has threshold `(d+2)/4 = 1`, exactly the true dissipation, so that comparison is **marginal and decides nothing**; since 2D Navier-Stokes is globally regular, the verdict for the maximal regime at `d = 2` is inconclusive rather than passing. The mechanism does not use vortex stretching, which is why it is not yet a Navier-Stokes mechanism (see T2).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2.9 axisymmetric swirl-free                  | not applicable                                                                                                                                 | No swirl in a shell model; same caveat as 2.8.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2.10 hyperdissipative regularity, `s >= 5/4` | passes                                                                                                                                         | Measured: the stall law's divergence point is `s* = 0.845`, consistent with `5/6` at the few-percent level (C8), far below `5/4`. Predicted: the maximal (front) cascade has threshold exactly `5/4` by the C10 corollary — but that corollary is CONJECTURE (it applies the power-law lemma under the regime hypothesis), so "the mechanism saturates Lions' exponent and cannot violate it, in any dimension" is withdrawn as a structural theorem and kept as a conjecture with the `d = 2` maximal case explicitly marginal (2.8). What is proved here is the exponent arithmetic: `b = 0` gives `s < (d+2)/4`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2.11 non-uniqueness of weak solutions        | not applicable                                                                                                                                 | All solutions here are classical solutions of a locally Lipschitz ODE system; uniqueness is automatic on the interval of existence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2.12 instantaneous Type I blowup             | not applicable                                                                                                                                 | Same reason.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2.13 Euler blowup (Elgindi, Chen-Hou)        | not applicable                                                                                                                                 | The inviscid model of C6 is a shell model, not Euler; no inference is drawn from one to the other.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2.14 unstable singularities via PINNs        | not applicable, methodological note                                                                                                            | The blowup located here is stable and elementary; the methodological bar of 2.14 (self-similar formulation, unstable-manifold count, plan for rigorous linearized bounds) applies to any attempt to promote C11's gadget to a Navier-Stokes profile, and is not met.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2.15 standing constraints, dyadic models     | passes                                                                                                                                         | The dyadic-model item of 2.15 is exactly this line's object. C7 measures stalls at `s = 1` in every truncated run and states the global-regularity statement about the untruncated model as CONJECTURE, because Barbato-Morandin-Romito's exponent range and solution class were never read and Cheskidov's stated global-regularity threshold, under this file's inferred dictionary, is `s >= 5/4` and so does not reach `s = 1`. C8 adds the quantitative law, which depends on no citation. The two papers are pointers for a reader with library access, not cross-checks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **T1 averaging test**                        | **passes as required**                                                                                                                         | The mechanism must apply to the averaged equation, and it does: C10 uses only the energy identity (C1), the scaling symmetry (C2) and the assumption that triad coefficients can be chosen — the properties Tao's `B~` is reported to retain [unverified: that paper was not read]. Conversely no regularity claim is made here that could survive T1, because every stall measured here uses positivity and the one-way flux structure, which averaging destroys (C12 point 1).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **T2 comparison systems**                    | **passes on hyperdissipation and on the 1D/Burgers comparison; inconclusive in 2D for the maximal regime; fails as a Navier-Stokes mechanism** | Hyperdissipative: the mechanism dies at `s >= 5/6` in the realized regime (measured, C8) and at `s >= 5/4` in the maximal one (C10, CONJECTURE), so it is dissipation-sensitive as required. Viscous Burgers, the comparison the earlier version omitted: the C1 energy identity does **not** separate the two systems — on the circle `int u^2 u_x = (1/3) int (u^3)_x = 0`, so 1D viscous Burgers has an energy identity of exactly this kind. (In `d > 1` the trilinear term `int (u.grad)u . u` equals `-(1/2) int (div u) abs(u)^2`, which need not vanish without incompressibility; the one-line argument that Burgers has no C1-type identity is therefore wrong in 1D, which is the case T2 actually names.) What separates them is the maximum principle `sup abs(u_i(t)) <= sup abs(u_i(0))`, which `00_problem.md` T2 records as the reason viscous Burgers is globally regular \[KNOWN, standard; `00_problem.md` marks it unverified as to a reference\] and which has no analogue in a shell model: the model has no maximum principle and no pointwise variable to apply one to. The scaling comparison is favorable (CONJECTURE, through the C10 regime hypothesis): the C10 corollary at `d = 1` gives thresholds `s < 3/4` (maximal regime) and `s < 1/2` (realized regime), both below the true dissipation `s = 1`, so the mechanism dies in 1D — as it must. 2D and swirl-free: the realized regime weakens correctly (`2/3 < 1`), while the maximal regime is exactly marginal at `d = 2` and therefore inconclusive (row 2.8). Finally, the mechanism nowhere uses vortex stretching or the pressure, so by T2 it is **not** a blowup mechanism for Navier-Stokes as it stands — recorded as a failure, and it is the same failure as Breakpoint 2. |
| T3 blowup profile constraints                | not applicable                                                                                                                                 | No velocity field, so `L^3` growth, BKM integrals, Leray rates and `P^1` measure of the singular set cannot be evaluated. Any future gadget-based scenario must be checked against all of T3 before it is called a candidate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| T4 inviscid-limit test                       | not applicable                                                                                                                                 | No `nu -> 0` claim is made; the inviscid model of C6 is studied on its own terms, and C7-C8 quantify how viscosity destroys the cascade at fixed `nu`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| T5 weak-solution test                        | not applicable                                                                                                                                 | See 2.11.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **T6 too-easy test**                         | **passes**                                                                                                                                     | The one result that would be too easy — "the dyadic model with the true Navier-Stokes dissipation is globally regular" — is **not claimed here as established**. What is claimed is COMPUTED and about truncated systems (C7), and the statement about the untruncated model is tagged CONJECTURE, precisely because the two citations that would make it KNOWN cannot carry that weight: Barbato-Morandin-Romito's exponent range and solution class are unverified, and Cheskidov's global-regularity threshold, under this file's inferred dictionary, is `s >= 5/4` and does not cover `s = 1`. Nothing here is obtained by an estimate, and no critical or subcritical a priori bound for Navier-Stokes is claimed anywhere in this line.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

## What would be needed next

- **Build the gate.** An explicit energy-conserving triad with a delayed switch, verified symbolically, chained so that the scaling map of C2 carries stage `n` to stage `n+1` with time contracted by `1/4`, and shown numerically to have front amplitude `A(n)` bounded below (slope `0`, not `-5/6`) and transfer times shrinking like `4^(-n)`. Under the C10 regime hypothesis such a gadget must then blow up for every `s < 5/4` and stop at `5/4`; since that hypothesis is exactly what is conjectural, the construction would be a test of the dichotomy and not merely an application of it — which is what makes it the sharpest next object in the ODE world.
- **Turn C11 into a theorem.** Prove that for every chain model with positive coefficients and scale invariance, the front amplitude obeys `limsup 2^(5n/6) A(n) < infinity`, i.e. the constant-flux profile is an upper barrier. This is presumably close to the mechanism of the Barbato-Morandin-Romito positivity proof and would explain, in one inequality, why one mode per scale can never suffice.
- **Resolve C6.** Decide whether `H^1` is finite at the inviscid escape time. The obstruction identified here is the truncation boundary layer — shell `N-1` has no drain, so the last several shells are not solutions of the infinite system and the fitting window still overlaps that layer at `N = 36` — so the fix is to remove the boundary from the measurement: integrate in the self-similar variables `tau = T* - t`, `n' = n - log_lam(1/tau)`, where the front is stationary and the truncation is not the limiting factor, or use an outflow condition at the last shell and check that the shoulder disappears.
- **Attack the port head on.** Compute, for a concrete family of divergence-free wave packets on `T^3` at frequencies `2^n`, the actual trilinear coefficients `<B(psi_a, psi_b), psi_c>` and ask how close a gate one can build from them — i.e. how small the unwanted interactions can be made relative to the wanted ones. A quantitative answer (even a negative one, of the form "the unwanted coefficients are at least a fixed fraction of the wanted ones") would convert Breakpoint 2 from a qualitative obstruction into a measured one, and that is the most useful next object this line points at.

## How to reproduce

```text
cd research/navier-stokes
python3 line_c_cascade.py
```

Requires `numpy`, `scipy`, `sympy`. Deterministic (no random numbers anywhere); total wall time about 165 s on 4 CPUs (two consecutive runs gave 160.3 s and 166.1 s, with byte-identical output); all output is printed to stdout and no files are written. Part 1 is symbolic (sympy) and prints exact residuals, including the interior and boundary residuals of C2 and the power-law lemma of C10; parts 2-5 integrate the ODE systems with `scipy.integrate.solve_ivp`, method `Radau`, analytic Jacobian for the dyadic model, `rtol = 1e-9`, `atol = 1e-16`. Lint: `ruff check --line-length 120 line_c_cascade.py` and `ruff format --line-length 120 line_c_cascade.py` both pass clean.

## Corrections made in response to the round-1 referee report

The report is at `refutations/line_c_r1.md`. Nothing criticized has been deleted: each item below is either fixed with a corrected argument or computation, or kept with a weaker status and a note saying what broke. Where the referee accepted a weaker status than this file claimed, that status is adopted.

| item                                            | was                                                          | now                                                               | what changed                                                                                                                                                                                                                                                                            |
| ----------------------------------------------- | ------------------------------------------------------------ | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C10 regime dichotomy                            | PROVED-HERE                                                  | **CONJECTURE**, with a **PROVED-HERE** power-law lemma split off  | The lemma (`b < L` for a finite escape time, `b < L - 2s` to beat dissipation) is exact arithmetic and is checked symbolically in script part 1h; the regime hypothesis, the undefined `>>`/`~` and the false "Fact 1" are replaced. Everything that says "by C10" inherits CONJECTURE. |
| C2 scaling Proposition                          | PROVED-HERE, no lattice hypothesis                           | PROVED-HERE, bi-infinite lattice hypothesis stated                | False at the boundary shell of the half-lattice: residual `mu^2 a_0^2`, now printed by script part 1c. Corollary identities corrected to `E(b) = 2(E(a) - a_0^2)`, `H^1(b) = (H^1(a) - a_0^2)/2`.                                                                                       |
| C7/C9/Goal/2.15/T6 global regularity            | KNOWN (BMR 2011, Cheskidov 2008)                             | **CONJECTURE**, citation dependence stated at four points of use  | BMR's exponent range and solution class are unverified; under this file's inferred dictionary Cheskidov's global-regularity threshold is `s >= 5/4` and does not cover `s = 1`.                                                                                                         |
| C6 `H^1` column                                 | "`H^1` just before escape = 2.0933 for every `N`"            | relabelled, plus an event-state column, both marked uninformative | `2.0933` is the value at the fixed grid time `t = 0.0820928`, identical across `N` by construction; the event-state value is dominated by `4^(N-1) esc_tol^2` from `N = 28` on. Both are printed by the script now.                                                                     |
| C6 drift diagnosis                              | FAILED, cause "the `-5/2` precursor tail"                    | FAILED, cause **the truncation boundary layer**                   | The old cause has the wrong sign; the `N = 32` event profile shows a non-monotone shoulder over the last ten shells, printed by script part 2.                                                                                                                                          |
| C8 precision                                    | "locates the threshold to about one percent"                 | "consistent with `5/6` at the few-percent level"                  | Slope `+4.0%`, intercept `+5.4%`, four integer-valued points extrapolated below their range; leave-one-out `s*` spans `0.8338 .. 0.8550`, now printed.                                                                                                                                  |
| C8 `s = 0.7` row                                | "truncation-independent, the front genuinely escapes"        | monotone drift, same signature as `s = 0.8`                       | Script part 4c prints both sweeps; the arbitrary 1% convergence test is removed. The prose sequence for `s = 0.8` that no script produced is replaced by printed numbers.                                                                                                               |
| C9 "regular throughout `s > 5/6`"               | stated as regularity                                         | **CONJECTURE**                                                    | Every truncated run is a bounded-energy finite ODE; it cannot exclude blowup of the infinite system.                                                                                                                                                                                    |
| C9 Lions justification                          | "its proof covers dyadic analogues"                          | withdrawn, replaced by C10's own arithmetic                       | A statement about an unread proof, and not literally true.                                                                                                                                                                                                                              |
| C6 Katz-Pavlovic tag                            | KNOWN as a theorem                                           | **COMPUTED**; two identifications flagged unverified              | Their model = this model with `lam = 2^(5/2)`, and their blowup = the escape event here; neither was checked. Same for the Kiselev-Zlatos entry.                                                                                                                                        |
| C10 dimension corollary at `d = 2`              | "T2 passing in a quantitative form"                          | realized regime passes; maximal regime **inconclusive**           | `(d+2)/4 = 1` at `d = 2` is exactly the true dissipation, so the comparison is marginal.                                                                                                                                                                                                |
| C12 point 1, Breakpoint 2                       | KNOWN, "the geometry does not permit"                        | **CONJECTURE**; the impossibility sentence withdrawn              | Individual Euler triad coefficients do vanish (`P_k` kills longitudinal vectors; `u_p . q` can vanish); what is true is that the family is not freely choosable.                                                                                                                        |
| T2 Burgers comparison                           | missing                                                      | run, and the naive argument corrected                             | 1D Burgers *does* have a C1-type energy identity on the circle; what separates it is the maximum principle, and the `d = 1` scaling thresholds `3/4` and `1/2` are both below `s = 1`.                                                                                                  |
| Fluid-computer exclusion                        | declined as [unverified]                                     | **PROVED-HERE**, one line                                         | Unforced steady Navier-Stokes on `T^3` forces `u` constant; a nonzero time-independent force violates (10).                                                                                                                                                                             |
| 2.1 "force the constant-flux profile"           | "force"                                                      | "select"                                                          | Forcing is the open problem listed under "What would be needed next".                                                                                                                                                                                                                   |
| C1 script description                           | "1a/1b with `lam`, `nu`, `s` free"                           | corrected, and the script changed to match                        | Part 1b used `lam^(2n)`, i.e. `s = 5/2`; it now uses `2^(2 s n)` with `s` symbolic.                                                                                                                                                                                                     |
| C4 analogy to 2.3                               | "the model's analogue"                                       | a reading, with the difference stated                             | The divergence is at large scales and violates `a_{-1} = 0`.                                                                                                                                                                                                                            |
| C11 heuristic step                              | "informally `c_{j+1} << c_j`"                                | marked a non sequitur                                             | It drops the amplitudes; Breakpoint 1 no longer restates it as fact.                                                                                                                                                                                                                    |
| C7 `E_end` entries, positivity print, `max H^1` | one truncation reported; stale `-1e-15`; unqualified maximum | both truncations; measured `-4.12e-19`; grid maximum stated       | Bookkeeping.                                                                                                                                                                                                                                                                            |
| `T*` seventh digit, scan 4a `n_d` offset        | `0.0828480`; "stall lands mid-range"                         | `0.082848` (six digits); offset variation explained               | `esc_tol` sweep printed by script part 2; the additive constant swings near threshold.                                                                                                                                                                                                  |

## References

- N. H. Katz, N. Pavlovic, "Finite time blow-up for a dyadic model of the Euler equations", Trans. Amer. Math. Soc. 357 (2005) [checked via WebSearch: title and the attribution of inviscid dyadic blowup; page numbers unverified; the paper was not read]. **Used in C6 as a pointer only.** Applying it to the escape measured here would require two unverified mathematical identifications — that their model is this model with `lam = 2^(5/2)`, and that their blowup is the event measured here — so C6 carries its own COMPUTED status and does not lean on this entry. Also N. H. Katz, N. Pavlovic, "A cheap Caffarelli-Kohn-Nirenberg inequality for the Navier-Stokes equation with hyper-dissipation", GAFA 12 (2002) [unverified].
- A. Kiselev, A. Zlatos, "On discrete models of the Euler equation", Int. Math. Res. Not. 2005, no. 38, 2315-2339 [checked via WebSearch: authors, title, journal, issue and pages, and the statement that the Katz-Pavlovic model blows up in finite time for any initial datum while the Obukhov model has global solutions for sufficiently smooth data]. The earlier description of the first half as "the theorem behind C6" is **withdrawn**, for the same two unverified identifications recorded in C6.
- A. Cheskidov, "Blow-up in finite time for the dyadic model of the Navier-Stokes equations", Trans. Amer. Math. Soc. 360 (2008), arXiv:math/0601074 \[checked via WebSearch: arXiv id, venue, and a snippet stating finite-time blowup for dissipation degree `alpha < 1/3`, local regularity for `alpha > 1/3`, global regularity for `alpha >= 1/2`, and the remark that `alpha = 1/3` has the same nonlinear estimates as 4D Navier-Stokes. The identification of that `alpha` with `2 s / 5` is inferred here, see "Setting and notation"\]. Under that inferred dictionary the global-regularity half is `s >= 5/4` and therefore does **not** cover the true dissipation `s = 1`; this is stated at each point of use (C7, transfer rows 2.15 and T6).
- D. Barbato, F. Morandin, M. Romito, "Smooth solutions for the dyadic model", Nonlinearity 24 (2011), 3083-3097, arXiv:1007.3401 [checked via WebSearch: title, venue, volume, pages, DOI 10.1088/0951-7715/24/11/004, and an abstract snippet stating well-posedness of positive solutions of the viscous problem in the scaling range corresponding to Navier-Stokes]. **The exact range of exponents, the solution class and the coefficient normalization are unverified**, so nothing in this file rests on them: C7's statement about the untruncated model is CONJECTURE, and this entry is a pointer for a reader who can check it.
- D. Barbato, F. Morandin, M. Romito, "Global regularity for a slightly supercritical hyperdissipative Navier-Stokes system", Analysis & PDE 7 (2014) \[checked via WebSearch; see also `00_problem.md` item 2.10\].
- T. Tao, "Finite time blowup for an averaged three-dimensional Navier-Stokes equation", J. Amer. Math. Soc. 29 (2016), 601-674, arXiv:1402.0290 \[checked via WebSearch, see `00_problem.md` item 2.2\]. The "self-replicating machine" description of the cascade, and the pump/rotor/transfer-gadget vocabulary used in C11, are from that paper and its accompanying blog post of 2014-02-04 [checked via WebSearch: the blog post exists; its detailed content is from memory, unverified].
- R. Cardona, E. Miranda, D. Peralta-Salas, F. Presas, "Constructing Turing complete Euler flows in dimension 3", Proc. Natl. Acad. Sci. USA 118 (2021) [checked via WebSearch: authors, title, venue, year, and the headline statement].
- S. Dyhr, A. Gonzalez-Prieto, E. Miranda, D. Peralta-Salas, "Turing complete Navier-Stokes steady states via cosymplectic geometry", arXiv:2507.07696 (2025), PNAS Nexus [checked via WebSearch: authors, title, arXiv id, venue, and the statement that universality holds on 3-manifolds admitting a nonvanishing harmonic 1-form].
- J.-L. Lions (1969), hyperdissipative regularity for `alpha >= 5/4`; P. Constantin, C. Fefferman (1993), direction of vorticity; J. T. Beale, T. Kato, A. Majda (1984); L. Escauriaza, G. Seregin, V. Sverak (2003): all as cited in `00_problem.md` items 2.10, 2.2, 2.7, 2.4 respectively, not re-verified here. In particular the earlier characterization of Lions' proof as "an energy-plus-interpolation argument" that "covers averaged and dyadic analogues" is withdrawn (C9): `00_problem.md` tags that item as the book's existence plus one attribution, and no claim in this file may rest on the internal structure of a proof nobody here read. The `5/4` used in C9 is this file's own number, from the power-law lemma of C10.
- A survey covering the whole dyadic-model literature exists — "Dyadic Models for Fluid Equations: A Survey", J. Math. Fluid Mech. (2023) [checked via WebSearch: title and venue appeared in search results; authors and content unverified, not used].
