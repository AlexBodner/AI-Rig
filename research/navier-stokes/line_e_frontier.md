# Line E — Frontier extension: logarithmically supercritical hyperdissipation

Status legend (one tag per claim): **PROVED-HERE** (complete proof written out), **KNOWN** (cited, not reproved), **COMPUTED** (script plus printed output below), **CONJECTURE**, **FAILED** (attempted, with the exact step where it breaks). Citation tags: [checked via WebSearch] / [unverified] as in `00_problem.md`.

## Citation frame — read before anything else in this file

**The `g^4`-versus-`g^2` target that organizes this whole line is a reconstruction from search snippets and could be wrong about what either cited paper proves.** No paper cited here was read (`00_problem.md` section 0: every route to the literature is blocked, and a [checked via WebSearch] tag confirms bibliographic existence, never a theorem statement). Three inferences are stacked to produce the target, none of them checkable in this environment, each flagged again at the point of use:

- (a) that the phrase "under the optimal condition on the correction to the dissipation" in the Barbato-Morandin-Romito abstract means Tao's integral condition `∫_1^∞ ds/(s g(s)^2) = ∞` rather than optimality in some other sense;
- (b) that the object of that 2014 Analysis & PDE paper is a Navier-Stokes system in the sense used here — its title says "system", and the *only* snippet describing a shell-iteration method belongs to a different, explicitly *dyadic*, companion paper;
- (c) that Tao 2009's hypothesis is `m(ξ) ≥ |ξ|^{(d+2)/4}/g(|ξ|)` with `∫_1^∞ ds/(s g(s)^4) = ∞`, in a domain and solution class comparable to the one used here (the snippet did not say `R^d` or torus, and said nothing about side conditions on `g`).

Each of (a), (b), (c) is **CONJECTURE** in this project's epistemics, not KNOWN. If (a) or (b) is wrong, the target of E6-E8 moves; if (c) is wrong, E5' reconstructs nothing. What does *not* depend on them is the internal mathematics: Theorem E1's a priori bound, Lemma E3 and Corollary E3', the diagonal case of (D), Proposition E5, the obstruction of Proposition E6 and the threshold computation of E7 are statements about explicitly displayed inequalities and stand or fall on their own proofs.

## Goal

The Lions exponent `s = 5/4` is the exact place where the 3D Navier-Stokes energy stops being supercritical (`00_problem.md` section 1.6), and the frontier just below it that has been pushed is a logarithmic one: Tao 2009 and Barbato-Morandin-Romito 2014 are *reported by search snippets* (see the citation frame; no statement was read) to prove global regularity for dissipation symbols `|ξ|^{5/2}/g(|ξ|)^2` weaker than `|ξ|^{5/2}` by a divergent logarithmic amount. Whether that is the only frontier ever pushed past a criticality threshold for this equation is a claim about the whole literature that this environment cannot check, and E2d's Colombo-Haffter result is a competing candidate that moves a genuine power for data of bounded size; so no superlative is claimed. This line does three things.

- **(E1)** It proves, in full, the Lions a priori `H^1` bound for `5/4 ≤ s ≤ 5/2` with the two independent reasons `5/4` is the threshold made explicit. The passage from that bound to *global regularity* is supplied entirely by (K1), an unread continuation criterion for fractional dissipation, so the globality statement is **KNOWN**, not proved here.
- **(E3-E7)** It redoes the logarithmic estimate from scratch with the weight kept explicit, and — conditionally on the unproved trilinear hypothesis (D) — delivers the condition `∫ds/(s g^4) = ∞`, which a snippet reports as Tao's hypothesis. It then proves that the specific split-at-`N_0` scheme of Proposition E5 can never deliver a condition weaker than `∫ds/(s g^4) = ∞`, for *any* weight `h` satisfying (H). That is an obstruction to **that scheme, under (D)**; the stronger reading "the whole family of weighted-enstrophy Gronwall schemes is incapable of reaching the `g^2` condition" is **CONJECTURE** and is not proved anywhere below.
- **(E4, E9)** It computes the cascade energy budget in which the exponent `2` on `g` appears. That budget is a statement about what the energy identity plus scaling *cannot rule out*, not about blowup: it **fails transfer test T2**, since the same four lines reproduce verbatim for 3D viscous Burgers and for swirl-free axisymmetric flow, both globally regular. Its `α`-general form does recover the Lions exponent as an integral test, and that part is exact.

The push is recorded as **FAILED**, with an obstruction that is proved at the level of the E5 scheme and conditional on (D). That is the honest outcome: the `g`-scale between `|ξ|^{5/2}/g^2` and `|ξ|^{5/2}` is *reported* to have been closed by Barbato-Morandin-Romito (citation frame (a),(b): unverified), and the argument below explains why this particular coercive-functional route cannot close it and what structure a successful route plausibly uses instead — "plausibly" because the method attribution in E8 comes from a snippet the notes themselves assign to a different paper.

## Setting and notation

Throughout, `T^3 = R^3/Z^3`, `u` is smooth, divergence-free and mean-zero, `Λ = (-Δ)^{1/2}`, and `P` is the Leray projector. The generalized (hyperdissipative) system is

```text
∂_t u + (u·∇)u = -ν L u - ∇p,    div u = 0,    u(·,0) = u^0,        (E-1)
```

where `L` is the Fourier multiplier with symbol `ℓ(ξ) ≥ 0`, `ℓ(ξ) = |ξ|^{2s}` in the pure fractional case and `ℓ(ξ) = |ξ|^{5/2}/g(|ξ|)^2` in the logarithmic case. `D` denotes the multiplier with symbol `m(ξ) = ℓ(ξ)^{1/2}`, so `⟨Lu,u⟩ = ‖Du‖_2^2`. Mean-zero is preserved and lets us use homogeneous norms `‖u‖_{Ḣ^σ} = ‖Λ^σ u‖_2` freely.

Littlewood-Paley: `u = Σ_{N} u_N` over dyadic `N = 2^j`, `j ≥ 0`, with the usual annulus projections. Bernstein on `T^3`: `‖u_N‖_{L^q} ≤ C N^{3(1/r - 1/q)}‖u_N‖_{L^r}` for `r ≤ q`, and `‖Λ^σ u_N‖_{L^r} ≍ N^σ ‖u_N‖_{L^r}`. The three bookkeeping quantities used below, all at fixed time, are

```text
E   = ‖u‖_{L^2}                     (energy, bounded by the energy identity)
A^2 = ‖Du‖_{L^2}^2                  (energy-level dissipation, in L^1_t by the energy identity)
Y_h = Σ_N h(N)^2 N^2 ‖u_N‖_{L^2}^2  (weighted enstrophy; h ≡ 1 gives Y = ‖∇u‖_2^2)
```

and the shorthand `a_N = N^{5/4}g(N)^{-1}‖u_N‖_2` (so `A^2 = Σ_N a_N^2` up to a fixed constant) and `c_N = h(N)N‖u_N‖_2` (so `Y_h = Σ_N c_N^2`).

Two standing KNOWN inputs, used but not reproved. **(K1) Local well-posedness and the continuation criterion.** For (E-1) with `ℓ(ξ) ≥ c|ξ|^{2}` and smooth periodic mean-zero data there is a unique smooth solution on a maximal interval `[0,T*)`, and if `T* < ∞` then `‖u(t)‖_{H^1} → ∞` as `t → T*`; hence an a priori `H^1` bound on `[0,T*)` forces `T* = ∞` (standard; for the hyperdissipative case see Lions 1969, Tao 2009 [unverified as to exact statement]). **(K2) The Sobolev product estimate.** For `a, b < 3/2` and `a + b > 0`, `‖fg‖_{Ḣ^{a+b-3/2}(T^3)} ≤ C_{a,b}‖f‖_{Ḣ^a}‖g‖_{Ḣ^b}` (standard paraproduct estimate [unverified as to exact reference]). **Gap, flagged at the point of use.** For `a + b - 3/2 < 0` and a product with nonzero mean the left side is `+∞` on the torus as literally written, so what step (4) of E1 actually needs for `s > 2` is the version with `fg` replaced by its mean-zero part, `‖fg - ∫fg‖_{Ḣ^{a+b-3/2}(T^3)} ≤ C_{a,b}‖f‖_{Ḣ^a}‖g‖_{Ḣ^b}`. That form is not stated in any source consulted here and no source was read; it is believed standard (it follows from the same Littlewood-Paley decomposition, whose low-frequency block is exactly the mean) but it is an **unverified** input, and Theorem E1 on `2 < s ≤ 5/2` is conditional on it.

## Results

| #   | Statement                                                                                                                                     | Status                                                                                                                                                                                                                                                  |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1  | The a priori bound (E1-8) for `(-Δ)^s` on `T^3`, `5/4 ≤ s ≤ 5/2`, and the two independent reasons `5/4` is the threshold                      | bound **PROVED-HERE** modulo (K2); the *global regularity* conclusion is **KNOWN** — it is supplied entirely by (K1), unread                                                                                                                            |
| E2  | Tao 2009 (`∫ds/(s g^4)=∞`), the conjecture and Barbato-Morandin-Romito 2014 (`∫ds/(s g^2)=∞`), Colombo-Haffter 2019 (`α ∈ (5/4-ε(M,δ), 5/4]`) | bibliography **KNOWN**; the mathematical content of each attribution is **CONJECTURE** (snippets only; citation frame)                                                                                                                                  |
| E3  | Osgood lemma plus the change of variables that turns a `g^p` loss at frequency `N_0 ≍ Y^κ` into the condition `∫ds/(s g^p)=∞`                 | **PROVED-HERE**                                                                                                                                                                                                                                         |
| E4  | The cascade energy budget: four scaling lines whose ratio is `ΔE_j/E_j^{1/2} ≍ ν g(2^j)^{-2}`, and the same computation at general `α`        | the four `≍` lines and the general-`α` integral test **COMPUTED** (Part 1, exact); the exact decrement `(ν/2)g^{-2}` and the two "if and only if" consequences **CONJECTURE**; sharpness **CONJECTURE**; the budget **fails transfer test T2**          |
| E5  | Frequency-localized bookkeeping with the weight explicit: `d/dt Y_h ≤ C ν^{-1} g(N_0)^4 Y_h A^2`                                              | the differential inequality **PROVED-HERE** under (D); (D) itself **PROVED-HERE** on the diagonal, **CONJECTURE** in general; the identification of the delivered condition with Tao's theorem (E5') is **CONJECTURE**                                  |
| E6  | The push to `g^2` by a weighted enstrophy: the weight cancels                                                                                 | **FAILED**. The obstruction — that the E5 split-at-`N_0` scheme delivers nothing weaker than `∫ds/(s g^4)=∞` for **any** `h` in (H) — is **PROVED-HERE** under (D). As a statement about *all* weighted-enstrophy Gronwall schemes it is **CONJECTURE** |
| E7  | The push via the commutator gain in the transport term: the absorption threshold is unchanged                                                 | **FAILED**. The threshold computation is **PROVED-HERE** modulo (K3) (transport piece only); the inference "produced independently by two estimates, so not an artifact" is **FAILED**                                                                  |
| E8  | Shell-flux iteration: the structure that can beat the per-frequency threshold                                                                 | **CONJECTURE** with proof plan; its attribution to BMR is **CONJECTURE** (the method snippet describes the dyadic companion paper)                                                                                                                      |
| E9  | Script output: exponent arithmetic, weight cancellation, Osgood dichotomy, budgets, dyadic shell model                                        | numbers **COMPUTED**; two readings withdrawn as **FAILED** (E9c's ODE columns discriminate nothing; E9e does not separate a power loss from criticality)                                                                                                |

### Status changes made in response to referee round 1

Nothing criticized has been deleted; every claim below is still in the file, at its new status, with a one-line note at the point of use saying what broke. `refutations/line_e_r1.md` is the report.

| claim                                                                     | was                            | is now                                                 | what broke                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------------------- | ------------------------------ | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1 "global and smooth"                                                    | PROVED-HERE                    | bound **PROVED-HERE** modulo (K2); globality **KNOWN** | steps (1)-(7) give only an a priori bound; `T* = ∞` is supplied entirely by the unread (K1)                                                                                                                                                                                                                                                    |
| E4's decrement `(ν/2)g^{-2}` and its two "iff" consequences               | PROVED-HERE                    | **CONJECTURE**                                         | a definite constant and a strict threshold cannot be read off `≍` relations, and the second "iff" is drawn where the dropped `O(ΔE^2/E^{3/2})` fails                                                                                                                                                                                           |
| E4 as evidence about blowup; the T2 row                                   | PROVED-HERE / "not applicable" | budget **fails T2**                                    | the four lines reproduce verbatim for viscous Burgers and swirl-free axisymmetric flow, both globally regular                                                                                                                                                                                                                                  |
| E5' "exactly Tao's hypothesis, recovered from scratch"                    | PROVED-HERE                    | **CONJECTURE**                                         | conditional on the unproved (D), and "exactly" asserts agreement with a theorem never read                                                                                                                                                                                                                                                     |
| E6 "the whole family of weighted-enstrophy Gronwall schemes is incapable" | PROVED-HERE                    | **CONJECTURE**                                         | E6 covers one scheme (majorant (D), split at `N_0`), under (D). *Strengthened in compensation:* it now covers **every** weight in (H), not just `h = N^a`                                                                                                                                                                                      |
| E7 "produced independently by two estimates"                              | PROVED-HERE                    | **FAILED**                                             | both derivations share the substitution of the global `Y_h^{1/2}` for a local quantity — the very loss E8 blames                                                                                                                                                                                                                               |
| T1's bolded ceiling                                                       | asserted                       | **CONJECTURE**                                         | rests on which bilinear estimates Tao's `B̃` preserves, the unread content of Tao 2016                                                                                                                                                                                                                                                          |
| 2.8 row, "in 2D at `s ≥ 1` by the same chain"                             | asserted                       | **FAILED**                                             | in `d = 2` the product estimate needs `a, b < 1`, so `a = 1` is inadmissible and only `s > 1` is reachable                                                                                                                                                                                                                                     |
| E9c "`p = 1.02` overflows at or before `t = 5`"                           | COMPUTED                       | **FAILED**                                             | the script prints `2.247e44`, finite; and the ODE columns do not separate `p = 1.00` from `p = 1.02` at all                                                                                                                                                                                                                                    |
| E9e "the model separates a power loss from criticality"                   | COMPUTED                       | **FAILED**                                             | the omitted row `x^{0.05}` (ratio `1.27`) sits between `log^{1/2}` and `log^{1}`; the rate is a spectral-slope diagnostic                                                                                                                                                                                                                      |
| "BMR's `g^2` condition", "the structure the successful route uses"        | KNOWN                          | **CONJECTURE**                                         | three stacked snippet inferences; see the citation frame                                                                                                                                                                                                                                                                                       |
| thirteen presentation defects (m1)-(m13)                                  | —                              | fixed in place                                         | mean-zero (K2) on `2 < s ≤ 5/2`, `N_0` at the lowest frequency, `≍` written as `=`, the dropped `E_j^{1/2}`, "without overflow", the (H)-violating `nan` row, incommensurable columns, "`g` of log type" now defined, E2d's quantifier inference, E2e as support, the script docstring, "smallest natural example", the historical superlative |

### E1. Lions: the a priori bound for `(-Δ)^s`, `5/4 ≤ s ≤ 5/2` — bound **PROVED-HERE**, globality **KNOWN**

**Theorem E1 (a priori bound) — PROVED-HERE modulo (K2).** Let `5/4 ≤ s ≤ 5/2` and `ν > 0`. Let `u` be the smooth solution of `∂_t u + (u·∇)u = -ν(-Δ)^s u - ∇p`, `div u = 0`, `u(·,0)=u^0` on its maximal interval `[0,T*)`, for smooth mean-zero divergence-free `u^0` on `T^3`. Then `‖∇u(t)‖_2` obeys the explicit bound (E1-8) below on `[0,T*)`, uniformly in `T*`; at `s = 5/4` the bound is uniform in `t` as well. (For `2 < s ≤ 5/2` this is conditional on the mean-zero form of (K2), see the gap flagged with (K2) above.)

**Corollary E1 (globality) — KNOWN, not proved here.** `T* = ∞`, so the solution is global and smooth. *This conclusion is not proved in this file.* Steps (1)-(7) below give only the a priori bound; the passage from a bounded `H^1` norm to `T* = ∞` is exactly (K1), a local well-posedness and continuation criterion for fractional dissipation whose statement was never read (\[unverified as to exact statement\]: which Sobolev index is propagated, whether the criterion is available at the borderline `s ≥ 5/4`, and whether it is stated on the torus are all unchecked). Per `00_problem.md` section 0 rule 1 the conclusion carries the status of its weakest input, and the weakest input is the one that delivers the conclusion. The result itself is KNOWN independently (`00_problem.md` item 2.10, Lions 1969).

Proof, in eight steps; every constant is named and every hypothesis of every cited inequality is checked. Write `E = ‖u^0‖_{L^2}`, `Y(t) = ‖Λu(t)‖_{L^2}^2`.

(1) **Energy identity.** Pair the equation with `u` in `L^2(T^3)`. `∫((u·∇)u)·u = (1/2)∫ u·∇|u|^2 = -(1/2)∫(div u)|u|^2 = 0`; `∫∇p·u = -∫ p div u = 0`; `⟨(-Δ)^su,u⟩ = ‖Λ^su‖_2^2`. Hence

```text
(1/2) d/dt ‖u‖_2^2 + ν ‖Λ^s u‖_2^2 = 0,   so   ‖u(t)‖_2 ≤ E   and   ∫_0^∞ ‖Λ^s u‖_2^2 dt ≤ E^2/(2ν).
```

(2) **Enstrophy identity.** Pair with `Λ^2 u`. The Leray projector `P` commutes with `Λ^2` and is self-adjoint with `Pu = u`, so `⟨Λ^2 u, P F⟩ = ⟨Λ^2 u, F⟩`; and `⟨Λ^2u,∇p⟩ = -⟨div Λ^2u, p⟩ = 0`. Hence, with `N := -⟨Λ^2u,(u·∇)u⟩`,

```text
(1/2) d/dt ‖Λ u‖_2^2 + ν ‖Λ^{1+s} u‖_2^2 = N.
```

(3) **Duality.** `div u = 0` gives `(u·∇)u = div(u⊗u)`, and `∫ div(u⊗u) dx = 0`, so its `Ḣ^σ` norms are defined for negative `σ` on the torus. By Plancherel `Λ^2 = Λ^{1+s}Λ^{1-s}` and `‖div F‖_{Ḣ^{1-s}} ≤ ‖F‖_{Ḣ^{2-s}}`, so

```text
|N| ≤ ‖Λ^{1+s}u‖_2 · ‖u⊗u‖_{Ḣ^{2-s}}.
```

(4) **Product estimate.** For `s > 2` the output index `2 - s` is negative, so first replace `u⊗u` by its mean-zero part `F := u⊗u - ∫_{T^3}u⊗u dx`; this changes nothing in step (3) because `div` annihilates constants, and it makes `‖F‖_{Ḣ^{2-s}}` well defined. All norms of `u⊗u` below are to be read as norms of `F`. **This is exactly where (K2) as stated does not literally apply** (its left side is the norm of the full product, which is `+∞` at a negative index when the mean is nonzero); on `2 < s ≤ 5/2` the step consumes the mean-zero form of (K2) flagged above, which is an unverified input. On `5/4 ≤ s ≤ 2` the output index is `≥ 0` and (K2) applies as stated. Apply (K2) with `a = 1`, `b = 5/2 - s`. Its hypotheses: `a + b - 3/2 = 2 - s` (the required output index, exactly); `a = 1 < 3/2`; `b < 3/2 ⟺ s > 1`, true since `s ≥ 5/4`; `a + b = 7/2 - s > 0` since `s ≤ 5/2`. Hence `‖u⊗u‖_{Ḣ^{2-s}} ≤ C_1‖u‖_{Ḣ^1}‖u‖_{Ḣ^{5/2-s}}`.

(5) **Interpolation.** For `5/4 ≤ s ≤ 5/2` one has `0 ≤ 5/2-s ≤ s`, so `5/2-s = θ s` with `θ := 5/(2s) - 1 ∈ [0,1]`, and `‖u‖_{Ḣ^{θ s}} ≤ ‖u‖_{L^2}^{1-θ}‖Λ^s u‖_2^{θ}` (Hölder on the Fourier side).

(6) **Young.** Combining (3)-(5) and `xy ≤ (ν/2)x^2 + y^2/(2ν)`,

```text
|N| ≤ C_1 E^{1-θ} ‖Λ u‖_2 ‖Λ^s u‖_2^{θ} ‖Λ^{1+s}u‖_2
     ≤ (ν/2)‖Λ^{1+s}u‖_2^2 + (C_1^2/(2ν)) E^{2(1-θ)} ‖Λ^s u‖_2^{2θ} · Y.
```

(7) **Gronwall.** With `φ(t) := (C_1^2/ν) E^{2(1-θ)}‖Λ^s u(t)‖_2^{2θ}`, steps (2) and (6) give `d/dt Y + ν‖Λ^{1+s}u‖_2^2 ≤ φ(t) Y`. By Hölder in time with exponents `1/θ` and `1/(1-θ)`, and then (1),

```text
∫_0^T φ ≤ (C_1^2/ν) E^{2(1-θ)} T^{1-θ} ( ∫_0^T ‖Λ^s u‖_2^2 dt )^{θ} ≤ (C_1^2/ν) E^{2(1-θ)} T^{1-θ} (E^2/(2ν))^{θ} < ∞.
```

(8) **Conclusion.** `Y(t) ≤ Y(0) exp(∫_0^t φ)` on `[0,T*)`, so `‖u(t)‖_{H^1}` is bounded on every bounded subinterval. **Everything up to here is proved; the next four words are not.** "By (K1), `T* = ∞`" is the whole of Corollary E1, and (K1) is unread; nothing in steps (1)-(7) yields `T* = ∞`. Explicitly, at `s = 5/4` (`θ = 1`) the a priori bound is time-uniform:

```text
(E1-8)    ‖∇u(t)‖_2^2 ≤ ‖∇u^0‖_2^2 · exp( C_1^2 ‖u^0‖_2^2 / (2ν^2) )   for all t ≥ 0.       ∎
```

**Where `s = 5/4` enters — two independent places, both verified exactly in Part 1 of the script.**

- *Time integrability.* `θ = 5/(2s) - 1 ≤ 1` iff `s ≥ 5/4`. The energy identity puts `‖Λ^su‖_2^2` in `L^1_t` and nothing better; `‖Λ^su‖_2^{2θ}` is in `L^1_t(0,T)` for `θ ≤ 1` and is not controlled for `θ > 1`. Step (7) is the only step that fails for `s < 5/4`.
- *Scaling.* Under `u ↦ λ^{2s-1}u(λ·)` the quantity `‖u‖_{Ḣ^σ}^2` has degree `d(σ) = 4s - 5 + 2σ`. The Gronwall coefficient in (6) has degree `d(s) + d(1) = 10s - 8`; the enstrophy dissipation has degree `d(1+s) = 6s - 3`; they agree iff `s = 5/4`. So `5/4` is exactly the exponent at which the Gronwall factor can be the energy dissipation itself, with no room to spare and no loss taken; `s > 5/4` is genuinely subcritical and the `T^{1-θ}` factor in (7) is the price of the slack.

**Not proved here.** The globality statement itself (Corollary E1), which is (K1) plus the bound; on `2 < s ≤ 5/2`, the mean-zero form of (K2) used in step (4). The range `s > 5/2` (choose `a = b = (7/2-s)/2 < 3/2` in step (4) and interpolate both factors; strictly easier, **KNOWN**). The whole-space version (the same proof works verbatim for Schwartz data on `R^3`; the only torus-specific point is the mean-zero convention in step (3)). Both (K1) and (K2) are used, not reproved.

### E2. The logarithmic frontier: Tao 2009, Barbato-Morandin-Romito 2014, Colombo-Haffter 2019 — **KNOWN**

**E2a (Tao 2009).** Consider `∂_t u + (u·∇)u = -D^2 u - ∇p`, `div u = 0` in dimension `d ≥ 3` (the snippet did not say whether the paper is stated on `R^d` or on the torus; the estimates involved are the same), where `D` is the Fourier multiplier with symbol `m(ξ)`. If `m(ξ) ≥ |ξ|^{(d+2)/4}/g(|ξ|)` for all sufficiently large `|ξ|`, for some nondecreasing `g:R^+ → R^+` with `∫_1^∞ ds/(s g(s)^4) = +∞`, then Schwartz data give a global smooth solution. The example given in the paper is `m(ξ) = |ξ|^{(d+2)/4}/log(2+|ξ|^2)^{1/4}`. In `d = 3` this is `m(ξ) = |ξ|^{5/4}/g(|ξ|)`, dissipation symbol `|ξ|^{5/2}/g(|ξ|)^2`. **Flag (citation frame (c)).** The tag below carries a *mathematical statement*, not a bibliographic fact, which is the case `00_problem.md` section 0 forbids relying on: the domain, the solution class and any side conditions on `g` are unknown, so every downstream sentence that says "Tao's hypothesis" means "the hypothesis a search snippet attributes to Tao" and carries status **CONJECTURE**. Source: T. Tao, Analysis & PDE 2 (2009) 361-366, arXiv:0906.3070. \[checked via WebSearch: the hypothesis `m(ξ) ≥ |ξ|^{(d+2)/4}/g(|ξ|)`, the condition `∫_1^∞ ds/(s g(s)^4) = +∞`, and the example `log^{1/4}(2+|ξ|^2)` were all returned verbatim in a search snippet; the proof was not read.\]

**E2b (the conjecture, and Barbato-Morandin-Romito 2014).** On the basis of a heuristic comparing the speed of propagation of a putative blowup up the frequency axis with the rate of dissipation, Tao conjectured that regularity should hold under the weaker condition `∫_1^∞ ds/(s g(s)^2) = +∞`. Barbato, Morandin and Romito proved global existence of smooth solutions "under the optimal condition on the correction to the dissipation". **That this settles Tao's conjecture — i.e. that "the optimal condition" is `∫ds/(s g^2) = ∞` — is an inference from two separate snippets and is CONJECTURE here, not KNOWN** (citation frame (a)); so is the identification of the paper's object with the Navier-Stokes equations rather than a model system, since its title says "system" and the equations were never seen (citation frame (b)). Both matter downstream: an obstruction to a scheme for the true equation is not in tension with a theorem about a dyadic model, and E6 and E8 are stated against this target. Source: D. Barbato, F. Morandin, M. Romito, Analysis & PDE 7 (2014) 2009-2027, arXiv:1407.6734. \[checked via WebSearch: the venue, volume, pages, the phrase "under the optimal condition on the correction to the dissipation", the attribution to Tao's conjecture, and the form `∫_1^∞ ds/(s G(s)^2) = ∞` of that conjecture together with its heuristic justification were all returned in search snippets; the proof was not read.\] A search snippet also describes the method: "an iterative estimate on the amount of energy stored in all shells larger than `j` at time `t` and the energy dissipated by these shells up to time `t`" [checked via WebSearch; the sentence describes the companion dyadic paper, D. Barbato, F. Morandin, M. Romito, "Global regularity for a logarithmically supercritical hyperdissipative dyadic equation", Dynamics of PDE 11 (2014) 39-52, arXiv:1403.2852 — checked via WebSearch: title, venue, volume, pages].

**E2c (what is between the two).** With `g(s) = log^{β}(2+s)`, `∫ ds/(s g^4) = ∞` iff `β ≤ 1/4` and `∫ ds/(s g^2) = ∞` iff `β ≤ 1/2`. So the gap between the two conditions is `β ∈ (1/4, 1/2]`, and `g = log^{1/2}`, i.e. dissipation symbol `|ξ|^{5/2}/log(2+|ξ|)`, is its endpoint: among `g = log^β` it is the *weakest dissipation* (largest `β`, hence the roundest exponent) that BMR are reported to cover and Tao 2009 is not. It is not the "smallest" example in the gap — `β` just above `1/4` is smaller in every sense; earlier drafts said "smallest", which was wrong. Table A of Part 4 of the script tabulates the partial sums and confirms the split. **COMPUTED**.

**E2d (the only known route strictly below the Lions power).** M. Colombo, S. Haffter, "Global regularity for the hyperdissipative Navier-Stokes equation below the critical order", arXiv:1911.02600, J. Differential Equations (2020): for every `M` and every `δ > 0` there is an explicit `ε = ε(M,δ) > 0` such that for divergence-free `u^0` with `‖u^0‖_{H^δ} ≤ M`, the fractional Navier-Stokes system with order `α ∈ (5/4 - ε, 5/4]` has a unique smooth solution; this comes from a stability statement saying that the set of pairs (datum, order) giving smooth solutions is open in `H^{5/4} × (3/4, 5/4]`. [checked via WebSearch: authors, title, arXiv id, and the abstract as paraphrased here.] The crucial point for this line: `ε` depends on the size of the datum, so this is *not* a statement about the equation with a fixed order `α < 5/4`; the class of data covered shrinks as `α` decreases. **Flag: that reading is a deduction from the order of quantifiers in a paraphrased abstract, not from the paper**, which was not read; it is likely right and it is **CONJECTURE** here. Nobody has a regularity theorem for a fixed `α < 5/4` with arbitrary data.

**E2e (the negative side of the same frontier).** "Non-uniqueness of weak solutions for a logarithmically supercritical hyperdissipative Navier-Stokes system", arXiv:2406.05853 [checked via WebSearch: title and arXiv id only; authors and contents not verified]. Its statement is unknown — the tag is a title and an arXiv id, nothing more — so **it confirms nothing and is cited only as a pointer**. The restriction of E1 and everything below to the smooth solution on `[0,T*)` rather than to arbitrary weak solutions (transfer test T5) is forced by the argument itself, not by this reference.

### E3. The Osgood lemma that converts a log loss into a divergence condition — **PROVED-HERE**

Every result in this area has the same shape: the enstrophy-type quantity satisfies a differential inequality with a logarithmically bad coefficient, and the divergence condition on `g` is exactly the Osgood condition making that inequality globally solvable. Isolating this makes it visible that the choice of `g^4` versus `g^2` is decided entirely upstream, in the estimate, and not by any cleverness in the Gronwall step.

**Lemma E3.** Let `y_0 > 0`, let `G:[y_0,∞) → (0,∞)` be nondecreasing, let `φ ≥ 0` be in `L^1_{loc}[0,∞)`, and let `y:[0,T*) → (0,∞)` be locally absolutely continuous with `y'(t) ≤ φ(t) y(t) G(y(t))` for a.e. `t` with `y(t) ≥ y_0`. If `∫_{y_0}^∞ dr/(r G(r)) = ∞` then `sup_{[0,T]∩[0,T*)} y < ∞` for every `T < ∞`.

Proof. Replace `y` by `ỹ = max(y, y_0)`, still locally absolutely continuous, still satisfying the inequality a.e. (on `{y < y_0}` the derivative of `ỹ` is `0 ≤ φ ỹ G(ỹ)`). Set `Ψ(z) = ∫_{y_0}^z dr/(rG(r))`. `Ψ` is continuous, strictly increasing (the integrand is positive), `Ψ(y_0) = 0`, and `Ψ(z) → ∞` as `z → ∞` by hypothesis; so `Ψ:[y_0,∞) → [0,∞)` is a bijection with increasing inverse `Ψ^{-1}`. By the chain rule for absolutely continuous functions, `(Ψ∘ỹ)'(t) = ỹ'(t)/(ỹ G(ỹ)) ≤ φ(t)` a.e., and `Ψ∘ỹ` is absolutely continuous, so `Ψ(ỹ(t)) ≤ Ψ(ỹ(0)) + ∫_0^t φ`. Applying `Ψ^{-1}`, `y(t) ≤ ỹ(t) ≤ Ψ^{-1}(Ψ(ỹ(0)) + ∫_0^T φ) < ∞` for `t ≤ T`. ∎

**Corollary E3'** (change of variables; this is why the condition is stated on `g` and not on `G`). Let `κ > 0`, `p > 0`, and suppose `c_1 g(r^κ)^p ≤ G(r) ≤ c_2 g(r^κ)^p` for `r` large, with `g ≥ 1` nondecreasing. Then `∫^∞ dr/(rG(r)) = ∞` if and only if `∫^∞ ds/(s g(s)^p) = ∞`. Proof: `∫^∞ dr/(r g(r^κ)^p) = (1/κ)∫^∞ ds/(s g(s)^p)` under `s = r^κ`, and multiplying `G` by a constant does not change convergence. ∎

So: an estimate that loses `g(N_0)^p` at the frequency `N_0 ≍ Y^{κ}` where the nonlinearity stops being absorbable yields exactly the condition `∫^∞ ds/(s g(s)^p) = ∞`, whatever `κ > 0` is. **The exponent `p` is the only thing that matters, and it is decided by the estimate, not by the Gronwall argument.** The scheme of E5 has `p = 4`, and so — according to the snippet in E2a, unverified — does Tao's theorem; the cascade budget of E4 has `p = 2`, and so, according to the reconstruction of E2b that the citation frame marks CONJECTURE, does Barbato-Morandin-Romito's theorem.

### E4. The cascade energy budget: where the exponent `2` on `g` comes from — the scaling lines **COMPUTED**, their two consequences **CONJECTURE**, the budget **fails transfer test T2**

This is the heuristic that Tao's conjecture rests on, written out with every exponent, because it is what tells us where the ceiling of the whole method is. Fix the dissipation symbol `ℓ(ξ) = |ξ|^{5/2}/g(|ξ|)^2` and consider the only blowup scenario that the energy identity plus scaling does not forbid: a *concentrating frequency cascade*, in which at stage `j` essentially all of the remaining energy `E_j` occupies the single dyadic shell `N_j = 2^j`.

**Computation E4 — COMPUTED** (Part 1 of the script recomputes the exponents symbolically). These are four `≍` relations between four displayed quantities, with unnamed implied constants; it is not a theorem about solutions, and — see the T2 paragraph below — it is not evidence about blowup either.

```text
energy in the shell            ‖u_{N_j}‖_2^2 = E_j
velocity amplitude             A_j ≍ N_j^{3/2} E_j^{1/2}          (Bernstein, saturated by a single bump of width 1/N_j)
eddy-turnover / transfer time  T_j ≍ (1/N_j)/A_j = N_j^{-5/2} E_j^{-1/2}
energy dissipated over T_j     ΔE_j ≍ ν ℓ(N_j) E_j T_j = ν N_j^{5/2} g(N_j)^{-2} · E_j · N_j^{-5/2} E_j^{-1/2}
                                    = ν E_j^{1/2} / g(N_j)^2
```

Since `E_{j+1} = E_j - ΔE_j`, a first-order expansion gives `E_{j+1}^{1/2} = E_j^{1/2} - ΔE_j/(2E_j^{1/2}) + O(ΔE_j^2/E_j^{3/2})` with `ΔE_j/(2E_j^{1/2}) ≍ ν/(2 g(N_j)^2)`. What that does and does not support:

- **What survives — COMPUTED.** The series governing the budget is `Σ_{j≥0} g(2^j)^{-2}`, and at a general dissipation order `α` it is `Σ_{j≥0} 2^{(2α-5/2)j}`; both exponents are exact and are recomputed symbolically in Part 1 of the script. The *dichotomy* convergence-versus-divergence is invariant under the unnamed constants of the four `≍` lines, and by the integral test `Σ_j g(2^j)^{-2} = ∞` iff `∫_1^∞ ds/(s g(s)^2) = ∞`.
- **What does not survive — CONJECTURE (two claims withdrawn).** Earlier drafts of this section asserted (i) that `E_j^{1/2}` decreases by *exactly* `(ν/2)g(2^j)^{-2}` per stage, and (ii) two "if and only if" statements: that the cascade reaches infinite frequency with energy left over iff `(ν/2)Σ_{j≥0}g(2^j)^{-2} < E_0^{1/2}`, and that it is drained at a finite stage for every datum and every `ν > 0` iff `Σ_j g(2^j)^{-2} = ∞`. Both are withdrawn to **CONJECTURE**. What broke, exactly: a definite constant `ν/2` and a strict inequality between two finite numbers cannot be read off `≍` relations, since the implied constants are what decide such an inequality; and the second "iff" is a statement about the regime `E_j → 0`, which is precisely where the dropped error `O(ΔE_j^2/E_j^{3/2})` ceases to be small relative to the retained term `ΔE_j/(2E_j^{1/2})`. Only the divergence dichotomy of the first bullet is constant-independent.
- The exponent `2` on `g` comes from one single place: `ΔE_j/E_j^{1/2}` is `ν ℓ(N_j) E_j^{1/2} T_j` and both `ℓ(N_j) = N_j^{5/2}g^{-2}` and `T_j ≍ N_j^{-5/2}E_j^{-1/2}` are pinned, the first by the definition of the equation and the second by Bernstein plus the energy normalization. Within this bookkeeping there is no freedom in it.

**Transfer test T2 — the budget fails it, and this is the main correction to this section.** Earlier drafts recorded T2 as "not applicable, no blowup mechanism is proposed". That was wrong on its face: the concentrating frequency cascade *is* a proposed blowup mechanism and carries the sharpness CONJECTURE below, so T2 applies and must be run. Running it: the four lines use only Bernstein, the energy identity and dimensional analysis, and none of those sees the pressure, the incompressibility constraint or vortex stretching. For 3D viscous Burgers `∂_t u + (u·∇)u = νΔu` (no pressure, no divergence constraint) the same four lines give the identical `A_j`, `T_j` and `ΔE_j/E_j^{1/2} ≍ νN_j^{-1/2}` at `α = 1`, summing to the same finite `ν(2+√2)` — yet Burgers is globally regular by the componentwise maximum principle (`00_problem.md` section 3, T2 list). The same holds verbatim for swirl-free axisymmetric 3D Navier-Stokes (item 2.9), globally regular, since no symmetry enters the four lines. **Conclusion: Computation E4 bounds what the energy identity plus scaling cannot rule out, and has no predictive content about actual blowup.** Consequently the word "natural" below is a statement about this bookkeeping only, and the sharpness CONJECTURE is weaker than it reads.

So `∫ ds/(s g(s)^2) = ∞` is the threshold this bookkeeping produces, while the scheme of E5 produces `∫ds/(s g^4) = ∞`; the factor of two is a property of the estimate, which E6 pins down. The earlier claim that `g^2` is "the ceiling of any argument built from the energy identity plus scaling" is **CONJECTURE**: no class of arguments is defined, and after T2 the budget is not even evidence that a cascade with a convergent series blows up. Barbato-Morandin-Romito are *reported* to reach the `g^2` condition (E2b; CONJECTURE per the citation frame).

**Sharpness — CONJECTURE, and weakened further by T2.** The converse, that `Σ_j g(2^j)^{-2} < ∞` permits an actual blowup (for the true equation, or even for an averaged or dyadic model), is not proved here and I could not confirm a theorem of that form. What is confirmed: A. Cheskidov, "Blow-up in finite time for the dyadic model of the Navier-Stokes equations", Trans. Amer. Math. Soc. 360 (2008) 5101-5120, proves finite-time blowup for the dyadic model when the dissipation degree is `α < 1/3` in his normalization \[checked via WebSearch: the venue, pages, and the threshold `α < 1/3`; the normalization was not verified, and it is *not* the 3D Navier-Stokes normalization — see the note in Part 4 Table B\]. That is a power-loss blowup, not a logarithmic one. The word "optimal" in the BMR abstract is the strongest evidence available here for logarithmic sharpness, and it is a word in an abstract, not a checked theorem.

**Relation to T1.** Everything in Computation E4 uses only the energy identity, Bernstein's inequality, and dimensional analysis. All three survive Tao's averaging (item 2.2) — and, as the T2 paragraph shows, they also survive the passage to systems that are globally regular. The two observations are the same observation: the budget is insensitive to everything that distinguishes these equations from each other, which limits what it can be evidence for in either direction.

### E5. The dyadic bookkeeping with the weight explicit: the scheme delivers `g^4` — the differential inequality **PROVED-HERE** under hypothesis (D); the identification with Tao's theorem **CONJECTURE**

Now redo the E1 estimate for `ℓ(ξ) = |ξ|^{5/2}/g(|ξ|)^2` with the logarithm kept explicit at every frequency, and with a general weight `h` in the coercive quantity, so that every weight satisfying (H) is covered at once. (That is a family of *weights* inside one fixed scheme — the majorant (D) with a single split point — not the family of all enstrophy-type Gronwall schemes.) The continuous version of E1 cannot be repeated: step (3) needs `‖Λ^{9/4}u‖_2`, and `‖Λ^{9/4}u‖_2^2 = Σ_N g(N)^2 · (N^{9/2}g(N)^{-2}‖u_N‖_2^2)` is not bounded by any multiple of the dissipation because `g` is unbounded. The frequencies must therefore be split, and the split point is the whole story.

**Standing hypotheses (H).** `g:[1,∞) → [1,∞)` nondecreasing with `g(2N) ≤ C_g g(N)`; `h` a positive weight on dyadic frequencies such that `φ(N) := N h(N)/g(N)^2` is nondecreasing with limit `+∞` and doubling constant `C_1` (i.e. `φ(2N) ≤ C_1 φ(N)`), and `N ↦ N g(N)^2 h(N)` is nondecreasing. Both hold for `h(N) = N^a`, `a > -1`, with `g` of log type.

**`g` of log type**, used in E5', E6 and E9b, means here: `g` is nondecreasing, `g ≥ 1`, `g(2s) ≤ C_g g(s)`, and `g(s^c) ≍_c g(s)` for every fixed `c > 0` (equivalently `log g(s) = o(log s)` together with doubling). `g = log^β(2+s)` satisfies it; a power `g = s^ε` satisfies doubling but *not* `g(s^c) ≍ g(s)`, and the power-loss rows of Part 2, whose spreads diverge, are exactly that boundary.

**The role of the doubling constant `C_1`, which earlier drafts left unsaid.** `C_1` is what keeps the scheme honest. Since `φ` is nondecreasing with limit `+∞`, doubling forces `C_1 > 1`, and `φ(2^j) ≤ C_1^j φ(1)` bounds how fast the split point can run away. Without it a weight such as `h(N) = e^N` would push `N_0` down to size `log Y_h`, and then `g(N_0)^4 ≍ g(log Y_h)^4` would make the delivered Osgood condition nearly vacuous — the scheme would appear to "prove" regularity for any `g`. Proposition E6 below is exactly the quantitative form of this remark.

**Notation.** `c_N = h(N) N ‖u_N‖_2`, `Y_h = Σ_N c_N^2`; `a_N = N^{5/4}g(N)^{-1}‖u_N‖_2`, `A^2 = Σ_N a_N^2 ≍ ‖Du‖_2^2`; and the weighted dissipation `B_h^2 = Σ_N h(N)^2 N^{9/2}g(N)^{-2}‖u_N‖_2^2 = Σ_N N^{5/2}g(N)^{-2} c_N^2`, which is what `⟨Λ^2 u, Lu⟩` supplies after weighting. The energy identity gives `∫_0^∞ A(t)^2 dt ≤ ‖u^0‖_2^2/(2ν)`, exactly as in E1(1).

**Hypothesis (D) (the diagonal trilinear bound).** Writing `T_h` for the nonlinear contribution to `d/dt Y_h`, assume `|T_h| ≤ C_0 Σ_N h(N)^2 N^{9/2} ‖u_N‖_2^3`.

*(D) holds for the frequency-diagonal part of the trilinear form — **PROVED-HERE**.* For `N_1 ≍ N_2 ≍ N_3 = N`, using `(u·∇)u = div(u⊗u)`, one integration by parts, Hölder with exponents `(2,4,4)`, and Bernstein `‖f_N‖_{L^4(T^3)} ≤ C N^{3/4}‖f_N‖_{L^2}`:

```text
|⟨Λ^2 u_N, div(u_{N_1}⊗u_{N_2})⟩| = |⟨∇Λ^2 u_N, u_{N_1}⊗u_{N_2}⟩|
      ≤ ‖∇Λ^2 u_N‖_2 ‖u_{N_1}‖_4 ‖u_{N_2}‖_4  ≤ C N^3 · N^{3/4} · N^{3/4} ‖u_N‖_2 ‖u_{N_1}‖_2 ‖u_{N_2}‖_2 = C N^{9/2}‖u_N‖_2^3.
```

*(D) is NOT proved here for the off-diagonal parts* (high-high to low, and high-low to high). The high-low-to-high part is treated separately in E7 and, remarkably, gives the same threshold; the high-high-to-low part is better behaved because the output frequency carries the small factor `N_3^3`. Everything below is therefore conditional on (D), and (D) is flagged as **CONJECTURE** as a statement about the full trilinear form.

**Two exact rewritings of the summand** (both verified symbolically in Part 1 of the script, residual `0`):

```text
(i)   h(N)^2 N^{9/2} ‖u_N‖_2^3 = ( N^{5/2} g(N)^{-2} c_N^2 ) · ( g(N)^2 ‖u_N‖_2 )
(ii)  h(N)^2 N^{9/2} ‖u_N‖_2^3 = ( N g(N)^2 h(N) ) · a_N^2 · c_N
```

**Proposition E5.** Assume (H) and (D). Let `N_0 = N_0(t)` be the least dyadic frequency with `φ(N) = N h(N)/g(N)^2 ≥ C_0 Y_h(t)^{1/2}/ν`. Then (E5-1) holds at every time, and (E5-2) holds at every time `t` with `Y_h(t) > (ν φ(1)/C_0)^2`:

```text
(E5-1)   d/dt Y_h + ν B_h^2  ≤  C_0 · ( N_0 g(N_0)^2 h(N_0) ) · Y_h^{1/2} · A^2
(E5-2)   d/dt Y_h            ≤  C_0^2 C_1 ν^{-1} · g(N_0)^4 · Y_h · A(t)^2.
```

Proof. `d/dt Y_h + 2ν B_h^2 = T_h`, and `|T_h| ≤ C_0 Σ_N h^2 N^{9/2}‖u_N‖_2^3` by (D). Split the sum at `N_0`.

*High frequencies `N > N_0`, via (i).* `‖u_N‖_2 = c_N/(h(N)N) ≤ Y_h^{1/2}/(h(N)N)`, so `g(N)^2‖u_N‖_2 ≤ g(N)^2 Y_h^{1/2}/(N h(N)) ≤ ν/C_0`, the last step being the definition of `N_0` together with the monotonicity of `N h(N)/g(N)^2` in (H). Hence `C_0 Σ_{N>N_0} (i) ≤ ν Σ_{N>N_0} N^{5/2}g(N)^{-2}c_N^2 ≤ ν B_h^2`, which the dissipation absorbs.

*Low frequencies `N ≤ N_0`, via (ii).* By the monotonicity of `N g(N)^2 h(N)` in (H) and `c_N ≤ Y_h^{1/2}`, `C_0 Σ_{N ≤ N_0} (ii) ≤ C_0 (N_0 g(N_0)^2 h(N_0)) Y_h^{1/2} Σ_N a_N^2`. That is (E5-1).

*From (E5-1) to (E5-2).* This step uses the *predecessor* `N_0/2`, which exists exactly when `N_0` is not the lowest dyadic frequency, i.e. exactly when `φ(1) < C_0 Y_h^{1/2}/ν`, i.e. when `Y_h > (νφ(1)/C_0)^2` — the restriction in the statement. Given that, minimality gives `φ(N_0/2) < C_0 Y_h^{1/2}/ν`, so by doubling `N_0 h(N_0)/g(N_0)^2 = φ(N_0) ≤ C_1 C_0 Y_h^{1/2}/ν`, i.e. `N_0 h(N_0) ≤ C_1 C_0 g(N_0)^2 Y_h^{1/2}/ν`. Substituting into (E5-1) turns `N_0 g(N_0)^2 h(N_0) Y_h^{1/2}` into `C_1 C_0 ν^{-1} g(N_0)^4 Y_h`. ∎

*The excluded regime is harmless.* On `{t : Y_h(t) ≤ (νφ(1)/C_0)^2}` the quantity `Y_h` is bounded by a constant depending only on `ν, C_0, h(1), g(1)`, so it can be added to the constant in any Osgood conclusion drawn from (E5-2). Earlier drafts said (E5-1) and (E5-2) both hold "at every time"; that was too strong, and this is the repair.

**Corollary E5' (Tao's condition falls out, with the right exponent).** Take `h ≡ 1`. Then `N_0 ≍ g(N_0)^2 Y^{1/2}/ν`, so for `g` of log type `log N_0 = (1/2)(1 + o(1)) log Y` and `g(N_0)^4 ≍ g(Y)^4` up to a constant. By Lemma E3 with `G(Y) = c g(Y^{1/2})^4` and Corollary E3' (with `κ = 1/2`, `p = 4`) and `∫_0^∞ A^2 dt ≤ ‖u^0‖_2^2/(2ν) < ∞`, the enstrophy `Y` does not blow up in finite time provided `∫_1^∞ ds/(s g(s)^4) = ∞`. **Status: PROVED-HERE modulo (D), (K1), (K2)** for the displayed conditional statement — "assuming (D), the scheme delivers the condition `∫ds/(s g^4) = ∞`".

**What this is not — correction.** Earlier drafts closed this corollary with "that is exactly Tao's hypothesis, recovered from scratch". Both halves are withdrawn to **CONJECTURE**. (1) Nothing is *recovered*: the derivation rests on hypothesis (D), which is unproved for the full trilinear form, so what is obtained is Tao's condition granted a trilinear majorant nobody has verified. (2) *Exactly* asserts agreement with a theorem never read. E2a's tag is a [checked via WebSearch] tag carrying a mathematical statement — the case section 0 forbids relying on — and it is silent on the domain (the snippet did not say `R^d` or torus), the solution class, and any side conditions on `g`. E5' is stated on `T^3` under (H); whatever Tao's theorem says, it is not stated under (H). The honest sentence is: *under (D), this scheme delivers the divergence condition with exponent `p = 4`, which is the exponent a search snippet attributes to Tao 2009.*

### E6. The push, attempt 1: weighted enstrophy. The weight cancels identically — **FAILED**; the obstruction is **PROVED-HERE** for the E5 scheme, under (D), for every weight in (H)

The push attempted here was to reach the natural `g^2` condition of E4 by choosing the coercive functional better: replace the enstrophy `‖∇u‖_2^2` by a weighted enstrophy `Y_h`, hoping that a weight growing like `g(N)^2` would soak up one of the two logarithmic losses. It does not work, and the reason is an exact cancellation rather than a near miss.

**Scope of what follows, stated before it rather than after.** Proposition E6 is an obstruction to **one scheme** — the pair (majorant (D), split at `N_0`) of Proposition E5 — and it is **conditional on (D)**, which is unproved and is deliberately cancellation-blind. An obstruction derived from a cancellation-blind majorant is an obstruction to arguments that use that majorant, not to arguments that use the trilinear form. The claim in earlier drafts that "the whole family of weighted-enstrophy Gronwall schemes is incapable of reaching the `g^2` condition" is **CONJECTURE** and is not proved here or anywhere below.

**Proposition E6 (the obstruction, for every weight in (H)) — PROVED-HERE under (H) and (D).** Let `g` be nondecreasing with `g(2s) ≤ C_g g(s)`, let `h` satisfy (H) with doubling constant `C_1` for `φ(N) = N h(N)/g(N)^2`, and set

```text
κ := log 2 / (2 log C_1) > 0     (C_1 > 1 is forced by (H): φ is nondecreasing with limit +∞)
```

Then the Gronwall coefficient of (E5-2), viewed as a function of `Y_h`, obeys `G(Y_h) = C_0^2C_1ν^{-1}g(N_0(Y_h))^4 ≥ c g(Y_h^{κ})^4` for all large `Y_h`, with `c > 0` depending only on `C_0, C_1, C_g, ν, h(1), g(1)`. Consequently

```text
∫^∞ dr/(r G(r))  ≤  C ∫^∞ dr/(r g(r^{κ})^4)  =  (C/κ) ∫^∞ ds/(s g(s)^4),
```

so **if `∫_1^∞ ds/(s g(s)^4) < ∞` then the Osgood integral of (E5-2) converges for every weight `h` in (H)**, Lemma E3 yields nothing, and the differential inequality (E5-2) is compatible with finite-time blowup of `Y_h` (its majorant ODE `y' = G(y) y A(t)^2` blows up in finite time for suitable data within the `L^1_t` budget `∫A^2 ≤ ‖u^0‖_2^2/(2ν)` that the energy identity allows). No choice of weight moves the split-at-`N_0` scheme outside the class `∫ds/(s g^4) = ∞`.

Proof. `C_1 > 1`: if `C_1 = 1` then `φ(2^j) ≤ φ(1)` for all `j`, contradicting `φ → +∞`. Iterating doubling, `φ(2^j) ≤ C_1^{j}φ(1)`. Write `N_0 = 2^{j_0}`. By the defining property `φ(N_0) ≥ C_0Y_h^{1/2}/ν`, so `C_1^{j_0} ≥ C_0Y_h^{1/2}/(νφ(1))` and

```text
log N_0 = j_0 log 2 ≥ (log 2/log C_1)·( (1/2)log Y_h - log(νφ(1)/C_0) ) = κ log Y_h - c_0 ,
```

with `c_0 = 2κ log(νφ(1)/C_0)` a constant. Hence `N_0 ≥ e^{-c_0} Y_h^{κ} ≥ 2^{-k} Y_h^{κ}` for the fixed integer `k = max(0, ⌈c_0/log 2⌉)`. Since `g` is nondecreasing and doubling, `g(Y_h^{κ}) ≤ C_g^{k} g(2^{-k}Y_h^{κ}) ≤ C_g^{k} g(N_0)`, so `g(N_0)^4 ≥ C_g^{-4k}g(Y_h^{κ})^4`. Multiply by `C_0^2C_1ν^{-1}`. The integral comparison is the substitution `s = r^{κ}` of Corollary E3'. ∎

**The matching upper bound for power weights.** For `h(N) = N^a`, `a > -1`, and `g` of log type the inequality above is an equivalence: the defining relation `N_0^{1+a}/g(N_0)^2 ≍ Y_h^{1/2}/ν` gives, after logarithms and `log g(N_0) = o(log N_0)`, `(1+a)log N_0 = (1/2)log Y_h + o(log N_0 + log Y_h)`, hence `κ = 1/(2(1+a))` (which is exactly what the general bound returns: `φ(2N)/φ(N) = 2^{1+a}g(N)^2/g(2N)^2 ≤ 2^{1+a}` because `g` is nondecreasing, so `C_1 = 2^{1+a}` and `κ = log 2/(2 log 2^{1+a}) = 1/(2(1+a))`) and `g(N_0)^4 ≍_a g(Y_h)^4`. By Corollary E3' with `p = 4` the Osgood condition delivered is *exactly* `∫_1^∞ ds/(s g(s)^4) = ∞` for every such `h`. For general `h` in (H) only the one-sided statement of Proposition E6 is proved, and that is the side the obstruction needs.

**Correction to an earlier sentence.** Earlier drafts said the coefficient is `C_0^2C_1ν^{-1}g(N_0)^4`, "in which the weight `h` appears only through `N_0`". That is false as written: `C_1` is the doubling constant of `φ(N) = Nh(N)/g(N)^2` and therefore depends on `h` too. The correct statement, and the one used above, is that `h` enters only through `N_0` and through the constants `C_1, φ(1)` — which is enough, because those constants only shift `κ` and `c`, not the divergence class.

*The motivating weight itself.* The obvious candidate is `h = g^2`, chosen precisely so that the `g^2` in the threshold cancels. It satisfies (H), and the defining relation becomes `N_0 h(N_0)/g(N_0)^2 = N_0 ≍ Y_h^{1/2}/ν`, with no logarithm left in it at all. But (E5-2) is unchanged: `g(N_0)^4 ≍ g(Y_h)^4` for `g` of log type, so the delivered condition is again `∫ds/(s g^4) = ∞`. The logarithm was removed from the threshold and reappeared, undiminished, in the coefficient — which is the cancellation of Proposition E6 seen from the other side.

**Where exactly the `g^2` is lost, and why no weight can recover it.** Trace the two factors of `g^2` in `g(N_0)^4`.

- One factor comes from rewriting (ii): converting the two energy-dissipation factors `a_N` back into `‖u_N‖_2` costs `g(N)^2`, because `a_N` measures `Du` and the estimate needs `Λ^{5/4}u = g(Λ)·Du`. This is unavoidable: `A^2` is the energy dissipation, and the energy identity supplies nothing else in `L^1_t`.
- The other comes from the absorption threshold, and this is the informative one. Divide the high-frequency summand by the dissipation at the same frequency: from (i), `(nonlinear at N)/(dissipation at N) = C_0 g(N)^2‖u_N‖_2/ν`. **The weight has already cancelled at this point, before any choice of `h` is made.** The absorption condition at frequency `N` is the `h`-free statement

```text
(E6-1)      ‖u_N‖_{L^2} ≤ ν / (C_0 g(N)^2).
```

That is a condition on the solution at one frequency; a weight in the coercive functional is a change of how frequencies are *summed*, and cannot alter a per-frequency ratio. Raising `h` makes the threshold `N_0` smaller for a given value of `Y_h`, but `Y_h` is correspondingly larger, and Proposition E6 says the two effects cancel to the accuracy that the Osgood condition can see. Part 2 of the script tabulates the *exponent bookkeeping* of this: for `h(N) = N^a` with `a ∈ {-1/2, 0, 1/2, 1}`, the coefficient `g(N_0)^4` varies by a factor that converges, as `Y → ∞`, to `((1+a_max)/(1+a_min))^{4β} = 4^{4β}` for `g = log^β` — `4.00` for `β = 1/4`, `16.06` for `β = 1/2` — i.e. by a bounded factor, and multiplying `g` by a constant does not change `∫ ds/(s g^p)`. **What Part 2 does not test.** Its four columns are compared at a common *numeric* value of `Y`, but `Y_h` for `h = N^{-1/2}, 1, N^{1/2}, N` are four different functionals of the same velocity field, so "the spread of the row" is not the ratio of coefficients that two schemes would see on one solution. The sentence just made — that the two effects cancel — is the content of Proposition E6, proved above; Part 2 checks the exponent arithmetic that the proof uses, and nothing more.

**The exact inequality that will not close, with its exponents.** To reach the `g^2` condition the low-frequency sum would have to obey

```text
(E6-2, target)    C_0 Σ_{N ≤ N_0} h(N)^2 N^{9/2}‖u_N‖_2^3   ≤   C ν^{-1} g(N_0)^2 · Y_h · A^2,
```

whereas the scheme proves only

```text
(E6-3, actual)    C_0 Σ_{N ≤ N_0} h(N)^2 N^{9/2}‖u_N‖_2^3   ≤   C_0 ( N_0 g(N_0)^2 h(N_0) ) Y_h^{1/2} A^2 = C ν^{-1} g(N_0)^4 · Y_h · A^2,
```

and the two differ by the factor `g(N_0)^2`, which by minimality of `N_0` satisfies `g(N_0)^2 ≥ (N_0h(N_0)ν)/(C_1C_0Y_h^{1/2})` — an inequality, not the equality earlier drafts wrote, since minimality bounds `φ(N_0)` from above and `φ(N_0/2)` from below and neither is an identity. Making the low-frequency sum smaller by `g(N_0)^2` would require the absorption threshold to be `N h(N) ≳ Y_h^{1/2}/ν` instead of `N h(N)/g(N)^2 ≳ Y_h^{1/2}/ν`, which by (E6-1) is false at a single frequency. **FAILED**, and — under (D), for this scheme, for every weight in (H) — the failure is a proved obstruction rather than an unfinished computation. It is also not repaired by choosing the split point elsewhere: moving it below `N_0` leaves non-absorbable frequencies in the Gronwall term, and moving it above `N_0` enlarges the supremum in the low-frequency sum; `N_0` is the optimum of (E5-1). What is *not* shown is that no other majorant for the trilinear form and no other split can do better: E7 tests one sharpening of (D) and finds the threshold unmoved, which is one data point, not a proof.

### E7. The push, attempt 2: commutator gain in the transport term does not move the threshold — **FAILED**

Hypothesis (D) is crude: it treats the trilinear form as if there were no cancellation. The natural next attempt is that the real trilinear form is smaller than (D) at high frequencies, so that the absorption threshold (E6-1) improves. The high-low-to-high interaction splits into transport of a high mode by a low mode, where the divergence-free condition produces a cancellation, and stretching of a low mode by a high one, where it does not; both are treated below. Neither helps, and the computation says why.

**Computation E7 — PROVED-HERE modulo the standard commutator estimate (K3).** Fix `M < N` dyadic and consider `T_{M,N} = -⟨Λ^2 u_N, (u_M·∇)u_N⟩`. Since `div u_M = 0`, `⟨Λ u_N, (u_M·∇)Λ u_N⟩ = 0`, so writing `Λ((u_M·∇)u_N) = (u_M·∇)Λ u_N + [Λ, u_M·∇]u_N`,

```text
T_{M,N} = -⟨Λ u_N, [Λ, u_M·∇] u_N⟩.
```

**(K3) KNOWN, in its frequency-localized form.** For `M ≤ N/8`, `‖[Λ, u_M·∇]u_N‖_{L^2} ≤ C ‖∇u_M‖_{L^∞}‖Λ u_N‖_{L^2}` [unverified as to exact reference]. This is the *localized* bound, not the general Kato-Ponce estimate, and the difference matters: the general one at `σ = 1` reads `‖[Λ,b]∂f‖_2 ≤ C(‖∇b‖_∞‖Λ f‖_2 + ‖Λ b‖_2‖∇f‖_∞)`, whose second term would contribute `M N^{7/2}‖u_M‖_2‖u_N‖_2^2` here — larger than the first by `(N/M)^{3/2}`, which would destroy the argument. That term is absent in the localized case because `û_M` is supported in `|η| ≍ M ≤ N/8` while `û_N` is supported in `|ξ-η| ≍ N`, so on the support of the product the commutator symbol `|ξ| - |ξ-η|` is `O(|η|) = O(M)`: all the growth in `ξ` cancels and only the first term survives. With Bernstein `‖∇u_M‖_∞ ≤ C M^{5/2}‖u_M‖_2`,

```text
|T_{M,N}| ≤ C ‖∇u_M‖_∞ · N^2 ‖u_N‖_2^2 ≤ C M^{5/2} ‖u_M‖_2 · N^2 ‖u_N‖_2^2.
```

This is a genuine gain over (D): the factor `N` that a naive bound would put on the transported field has been moved onto the low mode. Now weight and compare with the dissipation at frequency `N`, which is `ν N^{5/2}g(N)^{-2} c_N^2`; since `h(N)^2 N^2 ‖u_N‖_2^2 = c_N^2`, the weighted term is `C (Σ_{M<N} M^{5/2}‖u_M‖_2) c_N^2`, and

```text
Σ_{M<N} M^{5/2}‖u_M‖_2 = Σ_{M<N} M^{3/2} c_M / h(M) ≤ ( Σ_{M<N} M^{3/2}/h(M) ) · Y_h^{1/2} ≤ C N^{3/2} Y_h^{1/2}/h(N),
```

the last step by the geometric-sum dominance of the top term, valid whenever `M ↦ M^{3/2}/h(M)` is nondecreasing (true for `h = N^a`, `a < 3/2`). Absorption into the dissipation at frequency `N` therefore requires

```text
C N^{3/2} Y_h^{1/2}/h(N) ≤ ν N^{5/2} g(N)^{-2}      ⟺      N h(N)/g(N)^2 ≥ (C/ν) Y_h^{1/2},
```

**which is the same threshold `N_0` as in E5, with the same power of `g`.** The commutator gain of one derivative is exactly compensated by the fact that the low mode is measured in `L^∞` rather than `L^2`, and the `g(N)^2` survives untouched because it comes from the dissipation, not from the nonlinearity.

**The stretching piece, which earlier drafts omitted.** The other half of the high-low-to-high interaction is `T'_{M,N} = -⟨Λ^2u_N,(u_N·∇)u_M⟩`, for which no commutator is available and no cancellation is claimed. It needs none: by Cauchy-Schwarz, Hölder and Bernstein directly,

```text
|T'_{M,N}| ≤ ‖Λ^2u_N‖_2 ‖u_N‖_2 ‖∇u_M‖_∞ ≤ C N^2‖u_N‖_2^2 · M^{5/2}‖u_M‖_2,
```

which is *the same bound* as the commutator estimate gives for `T_{M,N}`. So the stretching piece contributes the same threshold and nothing changes — but the earlier assertion that transport is "the dominant high-frequency interaction", made without argument, was unsupported and is not needed: both pieces are the same size, and the term with no cancellation is the one that sets the ceiling.

**Conclusion — one inference withdrawn, one kept.** *Withdrawn (**FAILED**).* Earlier drafts concluded that the threshold "is produced independently by two quite different trilinear estimates ... so it is not an artifact of a lossy estimate". The two derivations are not independent at the step that fixes the threshold. In E5 the high-frequency branch substitutes `‖u_N‖_2 ≤ Y_h^{1/2}/(h(N)N)`; in E7 the low-mode sum is bounded by `(Σ_{M<N}M^{3/2}/h(M))·Y_h^{1/2}`. Both replace a *local* quantity by the *global* `Y_h^{1/2}` and then compare it against the dissipation at one frequency — which is exactly the lossy step the next paragraph goes on to blame. The agreement of the two thresholds is therefore evidence that the common conversion is what fixes them, which is the opposite of what was claimed. *Kept.* The threshold computation itself stands (**PROVED-HERE modulo (K3)**), and with E6 — no weight moves it — the reading that survives is that **what has to change to reach `g^2` is not the trilinear estimate and not the coercive functional, but the Gronwall structure itself**: the scheme compares a single scalar `Y_h(t)^{1/2}`, which knows only the total size of the solution, against the dissipation at each frequency separately, and therefore pays `g^2` at every scale. **FAILED**.

### E8. The push, attempt 3: shell-flux Gronwall (the direction that works in the literature) — **CONJECTURE** with proof plan

E6 and E7 together say the scalar Gronwall structure is the obstruction, at least for the majorant (D). The structure proposed to replace it is an iteration over shells rather than a single differential inequality. **Attribution flag (citation frame (b)) — this is where an earlier draft mis-cited.** The snippet describing "an iterative estimate on the amount of energy stored in all shells larger than `j` ... and the energy dissipated by these shells" is quoted in E2b with an explicit bracket saying it describes the **dyadic companion paper** (arXiv:1403.2852), not the 2014 Analysis & PDE paper (arXiv:1407.6734). Earlier drafts nevertheless said this is "exactly what Barbato-Morandin-Romito use" for the Navier-Stokes system. That attribution is **CONJECTURE**: a method confirmed (as far as a snippet confirms anything) for a dyadic model is here being assumed to be the method of a different paper about a system whose equations were never seen. The plan below is offered as a plan of mine, not as a reconstruction of theirs. I did not carry it out; writing it would exceed the two-page limit, so it is recorded as a plan.

**Conjecture E8 (proof plan, not a proof).** For dyadic `2^j` let

```text
E_{>j}(t) = Σ_{N > 2^j} ‖u_N(t)‖_2^2                        (energy above shell j at time t)
D_{>j}(t) = 2ν ∫_0^t Σ_{N > 2^j} N^{5/2} g(N)^{-2}‖u_N‖_2^2 ds   (energy dissipated above shell j up to t).
```

The frequency-localized energy identity gives `E_{>j}(t) + D_{>j}(t) = E_{>j}(0) + ∫_0^t Π_j(s) ds`, where `Π_j` is the energy flux through shell `j`, a trilinear expression supported on triads with at least one frequency `≍ 2^j`. The plan is to prove an iterative bound of the form `E_{>j+1} + D_{>j+1} ≤ (1 - c ν g(2^j)^{-2}) (E_{>j} + D_{>j}) + (lower order)`, so that after `J` steps the quantity above shell `J` is at most `exp(-cν Σ_{j<J} g(2^j)^{-2}) (E_{>0}+D_{>0})`, which tends to `0` as `J → ∞` precisely when `Σ_j g(2^j)^{-2} = ∞`; the energy above every shell then vanishes fast enough to give a subcritical bound and hence regularity by (K1).

**Why this can beat the scalar scheme, in one sentence.** The absorption threshold (E6-1) compares the dissipation at frequency `N` against `‖u_N‖_2 ≤ Y_h^{1/2}/(h(N)N)`, i.e. against the *total* size of the solution; the shell-flux scheme compares it against `E_{>j}^{1/2}`, the energy that has actually reached that part of the spectrum, which is itself already small because the preceding shells drained it — so the `g^2` from the threshold is paid once against a decreasing budget, rather than once per scale against a fixed one. This matches Computation E4 exactly, where the drain per stage is `ν g(2^j)^{-2}` applied to `E_j^{1/2}`.

**What I did not do.** The flux `Π_j` has a high-high-to-low part that is not obviously controlled by the two quantities above; the `(lower order)` term is not identified; and the passage from `E_{>j} → 0` at a quantified rate to a subcritical a priori bound is not written. Status **CONJECTURE**; the corresponding theorem is KNOWN (E2b) and the reconstruction here is not offered as a substitute for it.

### E9. Shell-model numerics for the cascade budget — **COMPUTED**

All four tables are produced by `line_e_frontier.py`; the numbers below are copied from its printed output.

**E9a — the exponent arithmetic of E1 and E5, exact in sympy (Part 1).** `d(σ) = 4s + 2σ - 5`; `d(s) + d(1) = 10s - 8`; `d(1+s) = 6s - 3`; equality iff `s = 5/4`. The E1 chain has `b = 5/2 - s`, `a + b - 3/2 = 2 - s` identically, `θ = (5/2-s)/s`, `θ ≤ 1` iff `s ≥ 5/4`, `θ ≥ 0` iff `s ≤ 5/2`. The nonlinear-to-dissipation ratio at frequency `N` in the E5 bookkeeping simplifies to `g(N)^2 ‖u_N‖_2` with the weight `h` absent, and the rewriting (ii) is verified with residual exactly `0`.

**E9b — the weight cancellation of E6 (Part 2).** `g(N_0)^4` for `h(N) = N^a`, and the ratio of the largest to the smallest entry in each row:

| `g`         | `log10 Y` | `a = -1/2` | `a = 0` | `a = 1/2` | `a = 1` | spread |
| ----------- | --------- | ---------- | ------- | --------- | ------- | ------ |
| `log^{1/4}` | 10        | 26.34      | 13.17   | 9.011     | 6.242   | 4.22   |
| `log^{1/4}` | 640       | 1481       | 740.3   | 493.5     | 370.1   | 4.002  |
| `log^{1/2}` | 10        | 930.2      | 211.9   | 94.17     | 48.07   | 19.35  |
| `log^{1/2}` | 640       | 2.217e6    | 5.532e5 | 2.456e5   | 1.38e5  | 16.06  |

The spread converges to `((1+a_max)/(1+a_min))^{4β} = 4^{4β}` (`4` and `16`), i.e. the weight changes the Gronwall coefficient only by a bounded factor, which the Osgood condition cannot see. This is the exponent arithmetic behind Proposition E6, not an independent test of it.

**Three honest caveats about this table, all visible in the run and now printed by the script.** (1) The frequency *search* is in log space (`N` up to `2^4000`); the reported coefficient `g(N_0)^4` is not, so the run emits `RuntimeWarning: overflow encountered in exp` and prints `inf` in three cells (`g = x^{0.10}` with `h = N^{-1/2}`, and `g = x^{0.25}` with `h = 1` and `h = N^{1/2}`). Earlier drafts said everything was "handled without overflow"; that was wrong. (2) The `nan` cells are the whole column `g = x^{0.25}`, `h = N^{-1/2}`, where `φ(N) = N h(N)/g(N)^2 = N^{1/2}/N^{1/2} ≡ 1` is constant: hypothesis (H) fails, no `N_0` exists, and that row is outside the scheme rather than a datum about it. (3) The search applies `np.maximum.accumulate` to the threshold function, which replaces a non-monotone `φ` by its running maximum and therefore *masks* (H) violations instead of reporting them. And, as said in E6, the four columns are compared at a common numeric `Y` although `Y_h` are different functionals; the table checks exponents only.

**E9c — the Osgood dichotomy of E3 (Part 3), with the reading corrected.** For `y' = y log(2+y)^p`, `y(0)=1`, the printed output is: `p = 0.5` reaches `t = 5` at `1.707e4` and `t = 20` at `9.948e48`; `p = 1.00` reaches `t = 5` at `6.836e35` and overflows before `t = 20`; **`p = 1.02` reaches `t = 5` at `2.247e44`** and overflows before `t = 20`; `p = 1.2, 2, 4` overflow already at `t = 5`. Truncated Osgood integrals at cutoffs `60` and `600`: `13.623 → 47.121` (`p=0.5`), `4.1166 → 6.4192` (`p=1.00`), `3.95 → 6.0235` (`p=1.02`), `2.7864 → 3.6001` (`p=1.2`), `0.89471 → 0.90971` (`p=2`), `0.19051 → 0.19051` (`p=4`).

**Correction — FAILED sentence, and what the table actually supports.** An earlier draft said "`p = 1.02, 1.2, 2, 4` overflow at or before `t = 5`". The script prints `p = 1.02` finite at `t = 5`; that sentence did not match the output and is withdrawn. Worse for the intended reading: `p = 1.00` (global by Osgood) and `p = 1.02` (blowup by Osgood) have the *same* signature in both `y` columns — finite at `t = 5`, overflow at `t = 20` — so the ODE columns discriminate nothing at the threshold, and "overflow" is not a blowup diagnostic (the script's legend, which said it was, has been corrected). The Osgood columns do not separate them either at these cutoffs (`4.1166 → 6.4192` versus `3.95 → 6.0235`); they separate only the coarse cases `p ≥ 2` (essentially constant) from `p ≤ 1.2` (growing), and that separation is a numerical restatement of Lemma E3 rather than independent evidence for it. **COMPUTED** for the numbers; **FAILED** as a discriminating test near `p = 1`.

**E9d — the budgets, and why the frontier is numerically invisible (Part 4, Table A).** Partial sums `S_p(M) = Σ_{m<M} g(2^m)^{-p}`:

| `g`         | `S_2(10)` | `S_2(10^2)` | `S_2(10^3)` | `S_2(10^4)` | `S_4(10)` | `S_4(10^2)` | `S_4(10^3)` | `S_4(10^4)` |
| ----------- | --------- | ----------- | ----------- | ----------- | --------- | ----------- | ----------- | ----------- |
| `1`         | 10        | 100         | 1000        | 1e4         | 10        | 100         | 1000        | 1e4         |
| `log^{1/4}` | 6.097     | 22.65       | 74.64       | 238.9       | 4.038     | 7.426       | 10.75       | 14.08       |
| `log^{1/2}` | 4.038     | 7.426       | 10.75       | 14.08       | 2.206     | 2.404       | 2.423       | 2.425       |
| `log^{1}`   | 2.206     | 2.404       | 2.423       | 2.425       | 1.117     | 1.119       | 1.119       | 1.119       |
| `x^{0.05}`  | 7.466     | 14.92       | 14.93       | 14.93       | 5.794     | 7.725       | 7.725       | 7.725       |

`g = log^{1/2}` is the endpoint of the gap between the two conditions — among `g = log^β` the *weakest dissipation* (largest `β`) inside the reported BMR hypothesis and outside the reported Tao hypothesis, the whole gap being `β ∈ (1/4,1/2]`: `S_2` still grows at `M = 10^4` while `S_4` has already converged to `2.425`. The divergent sums grow like `log M`, which is the quantitative form of the statement that the Tao/BMR frontier cannot be seen numerically.

**E9e — dyadic shell model (Part 4, Table B).** The model `a_n' = c_{n-1}a_{n-1}^2 - c_n a_n a_{n+1} - ν d_n a_n` with `c_n = λ^{β(n+1)}`, `λ = 2`, `β = 5/2`, `d_n = λ^{(2β/3)n}/g(λ^n)^2`, `ν = 0.05`, `a_0(0) = 1`. **The exponent is `2β/3`, not `β`**: the dyadic model's own criticality is set by its constant-flux (Kolmogorov) cascade `a_n ≍ λ^{-βn/3}`, not by the concentrating cascade of E4, so its critical dissipation order is `2β/3` and it is *not* the Lions exponent. `log10 max_t Σ_n λ^{2n}a_n^2` versus truncation `M`, and the growth rate in decades per shell (a constant-flux inertial range gives `log10 λ^{1/3} = 0.1003`):

| `g`         | `M=10` | `M=14` | `M=18` | `M=22` | dec/shell | vs inertial |
| ----------- | ------ | ------ | ------ | ------ | --------- | ----------- |
| `1`         | 2.361  | 2.732  | 3.098  | 3.462  | 0.0910    | 0.91        |
| `log^{1/4}` | 2.769  | 3.240  | 3.687  | 4.124  | 0.1091    | 1.09        |
| `log^{1/2}` | 3.175  | 3.735  | 4.249  | 4.739  | 0.1225    | 1.22        |
| `log^{1}`   | 3.974  | 4.706  | 5.341  | 5.926  | 0.1462    | 1.46        |
| `x^{0.05}`  | 2.635  | 3.140  | 3.646  | 4.156  | 0.1274    | 1.27        |
| `x^{0.10}`  | 2.910  | 3.546  | 4.182  | 4.820  | 0.1596    | 1.59        |
| `x^{0.25}`  | 3.733  | 4.743  | 5.747  | 6.751  | 0.2509    | 2.50        |

**Reading, with the previous one withdrawn.** Earlier drafts of this table printed six of the script's seven rows — the row `g = x^{0.05}` was missing — and read them as "the power losses sit well above the inertial rate, so at 22 shells the model separates a power loss from criticality". The omitted row is restored above and it refutes that reading: `x^{0.05}` has ratio `1.27`, which sits *between* `log^{1/2}` (`1.22`) and `log^{1}` (`1.46`). The model therefore does **not** separate power losses from logarithmic ones; the ratios simply increase with the size of the loss. That sentence is **FAILED**.

**And the diagnostic measures something weaker than it sounds.** The reported quantity is `log10 max_t Σ_n λ^{2n}a_n^2` as a function of the truncation `M`. For a perfectly regular constant-flux (Kolmogorov) solution `a_n ≍ λ^{-βn/3}` that partial sum already diverges with `M`, at exactly `log10(λ^{1/3}) = 0.1003` decades per shell (the balance `2α = 2β/3` recomputed by hand and printed by the script). So the "rate" is a *spectral-slope* diagnostic, not a blowup diagnostic: a larger rate means a shallower spectrum, and no value of it decides whether the model blows up. No resolution or tolerance study was run and it was not checked whether `max_t` is attained in the interior of `[0,60]`, so these numbers are unvalidated as numerics. What survives is the sentence the notes already ended with: **these numerics are not evidence for or against the divergence conditions**, they are about a shell model, and the shell model is not Navier-Stokes.

## Breakpoint

**Where this line stops short of the Clay problem, stated as a computation rather than as a mood.** Everything above is about a *modified* equation. Fefferman's (A)-(D) fix `ν Δ`, i.e. `α = 1`, and Lions' threshold is `α = 5/4`; the gap is a full power `|ξ|^{1/2}`, not a logarithm. Run Computation E4 at a general order `α`, with dissipation symbol `|ξ|^{2α}`:

```text
ΔE_j / E_j^{1/2} ≍ ν ℓ(N_j) E_j^{1/2} T_j = ν N_j^{2α} · E_j^{1/2} · N_j^{-5/2} E_j^{-1/2} = ν N_j^{2α - 5/2} = ν 2^{(2α-5/2) j}.
```

The total drain over the whole cascade is `ν Σ_j 2^{(2α-5/2)j}`, which **diverges iff `α ≥ 5/4` and converges iff `α < 5/4`** — the Lions exponent, recovered as an integral test. At `α = 1` the series is `ν Σ_j 2^{-j/2} = ν(2+√2)`, a *finite number of order `ν`*. So a concentrating cascade at `α = 1` loses only `O(ν)` of `E_0^{1/2}` in total, no matter how many decades it traverses; the energy identity does not drain it at all. **Read this as a statement about the energy identity, not about blowup:** the identical number `ν(2+√2)` comes out for 3D viscous Burgers and for swirl-free axisymmetric flow, which are globally regular (the T2 paragraph in E4). The computation says the energy identity cannot rule the cascade out; it does not say the cascade occurs. The logarithmic frontier of Tao and Barbato-Morandin-Romito lives entirely inside the borderline case `2α = 5/2` where that series is exactly `Σ_j g(2^j)^{-2}`, i.e. marginally divergent, and improving `g^4` to `g^2` moves the boundary from `log^{1/4}` to `log^{1/2}`. Measured against the missing power `|ξ|^{1/2}`, that is not progress towards `α = 1`; it is the sharpening of a boundary case.

**The internal breakpoint of the push.** The push (E6, E7) is **FAILED** at a specific inequality: to obtain the exponent `g^2` the low-frequency sum would have to satisfy (E6-2) whereas the scheme proves only (E6-3), the two differing by the factor `g(N_0)^2` that defines the absorption threshold. Everything in this paragraph is conditional on hypothesis (D) and concerns the split-at-`N_0` scheme only. That threshold is the per-frequency inequality `‖u_N‖_2 ≤ ν/(C_0 g(N)^2)` of (E6-1), in which no weight appears (E6) and which is unchanged by the commutator gain in the transport term (E7). What must change is the Gronwall structure — a scalar `Y_h(t)` compared against the dissipation at every frequency separately pays `g^2` at every scale — and the structure proposed to change it is the shell-flux iteration of E8, recorded as **CONJECTURE**, whose attribution to the literature is itself **CONJECTURE** (E2b and the citation frame: the method snippet describes the dyadic companion paper).

## Transfer check

Rows are the no-go items of `00_problem.md` section 2 and the transfer tests of section 3. "Object tested" is E1 plus E5-E7 (the regularity scheme) unless stated.

| Item                                  | Verdict                                                                               | Because                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2.1 supercriticality barrier          | passes, and is the point                                                              | The scheme uses only the energy identity plus scaling, which is exactly why it stops at a modified dissipation; at `α = 1` the Breakpoint computation shows the cascade drain is `O(ν)`, i.e. the barrier bites                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2.2 averaged blowup                   | passes                                                                                | The argument would apply verbatim to `∂_t u = -D^2 u + B̃(u,u)` (see T1); this is consistent, since Tao's blowup is at `α = 1` and the argument is empty there. Whether *every* estimate used survives averaging depends on the unread scope of Tao 2016 — see the T1 row                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2.3 no backward self-similar profiles | not applicable                                                                        | No blowup ansatz is proposed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2.4 `L^∞_t L^3_x` (ESS)               | not applicable                                                                        | Statements are for modified dissipation; ESS is an `α = 1` theorem                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2.5 LPS and Leray lower bounds        | passes                                                                                | E1 proves an `H^1` bound only for `s ≥ 5/4`, where `H^1` is strictly subcritical; nothing is claimed at `s = 1`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2.6 partial regularity (CKN)          | not applicable                                                                        | No local analysis is used                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2.7 Beale-Kato-Majda                  | not applicable                                                                        | No vorticity criterion is used                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2.8 2D global regularity              | passes vacuously; one earlier claim **FAILED**                                        | The line proves regularity, not blowup. An earlier draft added "in 2D the analogue of E1 holds at `s ≥ 1` by the same chain with `d = 2`". That is false at the endpoint and is withdrawn: in `d = 2` the product estimate (K2) needs `a, b < d/2 = 1` while step (3) forces `a + b = 3 - s`, so `a < 1` and `b < 1` give `s > 1` **strictly**, and E1's choice `a = 1` is exactly inadmissible. The scaling half is fine — with `d = 2`, `d(σ) = 4s + 2σ - 4`, `d(s)+d(1) = 10s-6`, `d(1+s) = 6s-2`, equal iff `s = 1` — so the threshold `s = 1` is right and the stated proof of it does not exist. "`s > 1` by the same chain" is **CONJECTURE** (not written out)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2.9 axisymmetric swirl-free           | not applicable                                                                        | No symmetry reduction is used                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2.10 hyperdissipative regularity      | this line IS this item                                                                | E1 proves the a priori bound for `s ≥ 5/4` and takes globality from (K1); E2 records the logarithmic half as bibliography whose content is unverified; E5 delivers the `g^4` condition **under hypothesis (D)**, proved here only on the frequency diagonal; E6-E7 record the failed push towards the condition that E2b attributes to BMR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2.11 non-uniqueness of weak solutions | passes                                                                                | Everything is about the unique smooth solution on `[0,T*)` from (K1); no statement is made about Leray-Hopf solutions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2.12 instantaneous Type I blowup      | passes                                                                                | Same reason; E1 step (8) uses smoothness at every `t < T*` through the continuation criterion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2.13 Euler blowup                     | passes                                                                                | Every estimate has a factor `ν^{-1}` (see (E1-8)) and degenerates as `ν → 0`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2.14 unstable singularities (PINN)    | not applicable                                                                        | No numerical profile search is attempted; E9e is a shell model, and its negative reading is reported                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2.15 critical spaces, dyadic models   | passes                                                                                | E9e's dyadic model is used only as an analogy and its *different* criticality (`2β/3`, not `β`) is stated explicitly                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **T1 averaging**                      | consistency reading **passes** (**PROVED-HERE**); the ceiling claim is **CONJECTURE** | E1, E4, E5, E6 use the energy identity, scaling, Bernstein/Littlewood-Paley, Hölder, the size bound (D) — and, in E1 steps (3)-(4), the duality and product structure of `div(u⊗u)`. The first five survive Tao's averaging, so the argument applies verbatim to the averaged equation with the same dissipation, which is not a contradiction: Tao's blowup is at `α = 1` and at `α = 1` the argument proves nothing (Breakpoint). That much is solid. **Withdrawn:** an earlier draft concluded in bold that "no strengthening of this line can ever reach an order at which the averaged equation blows up". Three defects: whether Tao's `B̃` admits the same `Ḣ^{2-s}` bound as `div(u⊗u)` is the unread content of Tao 2016 (item 2.2's own tag is a WebSearch abstract check); the consequent is nonvacuous only at `α = 1`, where the Breakpoint's convergent series already gives the conclusion unconditionally and more simply; and "no strengthening of this line" is not a defined class of arguments, so the sentence is not falsifiable. Also flagged, and still open: E7 uses the localized cancellation `⟨B(u_M,f),f⟩ = 0`, strictly stronger than the energy cancellation averaging preserves; whether `B̃` retains it is **CONJECTURE** |
| T2 comparison systems                 | **fails for E4**                                                                      | An earlier draft said "not applicable — no blowup mechanism is proposed". E4 does propose one (a concentrating frequency cascade) and carries a blowup sharpness CONJECTURE, so T2 applies. Running it: the four E4 lines use only Bernstein, the energy identity and dimensional analysis, none of which sees pressure, incompressibility or vortex stretching, so they reproduce verbatim for 3D viscous Burgers (globally regular by the componentwise maximum principle) and for swirl-free axisymmetric 3D Navier-Stokes (globally regular, item 2.9), giving in both the same finite drain `ν(2+√2)` at `α = 1`. E4 is therefore a statement about what the energy identity cannot rule out, with no predictive content about blowup. The regularity results E1/E5 are unaffected (a regularity proof is not required to fail on globally regular systems)                                                                                                                                                                                                                                                                                                                                                                                         |
| T3 blowup profile constraints         | not applicable                                                                        | Same                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| T4 inviscid limit                     | passes                                                                                | See 2.13                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| T5 weak solutions                     | passes                                                                                | E1 is a statement about the unique smooth solution; the `H^1` bound is propagated by Gronwall along that solution, and the continuation criterion (K1) is the only bridge to global existence. The restriction is forced by the argument itself. E2e is *not* offered as support: its statement is unknown (title and arXiv id only), and a paper whose statement is unknown confirms nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| T6 too-easy                           | passes                                                                                | E1 does close an `H^1` bound — but only for `s ≥ 5/4`, where it is Lions' 1969 theorem, KNOWN, not new. With `g ≡ 1` the E5 scheme collapses to E1 at `s = 5/4`, and with a power loss `g = s^ε` the Osgood integral converges, so the scheme correctly proves nothing below the Lions exponent. The doubling constant `C_1` in (H) is what blocks the free lunch: an absurdly fast weight such as `h(N) = e^N` would drive `N_0` down to `log Y_h` and make the delivered condition nearly vacuous, and (H) excludes it — this is exactly Proposition E6's `κ = log 2/(2 log C_1)`. The push E6-E7 does **not** close, and is recorded as FAILED with the exact inequality                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

## What would be needed next

- **Finish E8 or read BMR.** The shell-flux iteration is the only structure identified here that might beat the per-frequency threshold (E6-1); that it is the structure BMR use is an unverified attribution taken from a snippet about a *different* (dyadic) paper. The two missing pieces are a bound on the high-high-to-low part of the flux `Π_j` in terms of `E_{>j}` and `D_{>j}` alone, and the passage from a quantified decay of `E_{>j}` to a subcritical a priori bound. Both are presumably in arXiv:1407.6734, which this environment could not fetch.
- **Settle hypothesis (D) for the full trilinear form.** E5 and E6 are conditional on it. The high-high-to-low part should be strictly better (the output frequency supplies `N_3^3` with `N_3 ≪ N_1 ≍ N_2`), and E7 handles high-low-to-high, but neither is written as a complete paraproduct sum here.
- **Decide the sharpness question of E4 — now harder than it looked.** After the T2 failure, the budget is known to be blind to everything that separates Navier-Stokes from viscous Burgers, so a sharpness theorem cannot be expected to follow from it. The question stands on its own: is there a blowup theorem, for the averaged equation or for a dyadic model at its own critical order, when `Σ_j g(2^j)^{-2} < ∞`? If yes, then the `g`-scale is provably closed and this whole direction is finished; if no, then "optimal" in the BMR abstract is a statement about the method, not about the equation, and there may be room below `g^2`. I could not resolve this from search snippets.
- **Does Tao's averaged operator retain `⟨B̃(u_M,f),f⟩ = 0`?** Rotation conjugation preserves it; conjugation by a non-unitary order-zero multiplier need not. If it does not, then E7 (and any argument using the localized transport cancellation) is *not* abstract in the T1 sense, and the T1 ceiling for such arguments is higher than stated. **CONJECTURE**, worth one page.
- **The Colombo-Haffter direction (E2d) is the only one that moves the power.** The interesting question it raises is whether the `ε(M,δ)` can be made independent of `M` for data in a fixed *critical* ball rather than a subcritical one; if so the argument would iterate. Nothing here bears on it.

## How to reproduce

```text
cd research/navier-stokes
python3 line_e_frontier.py            # about 3 minutes on 4 CPUs (under the 5-minute cap); prints Parts 1-4 in order
ruff check --line-length 120 line_e_frontier.py
ruff format --check --line-length 120 line_e_frontier.py
```

The script is deterministic (a seed is set and printed but no randomness is used) and takes about 3 minutes. Part 1 is sympy, exact. Part 2 searches for the split frequency in log space, so frequencies up to `2^4000` can be examined; the *reported* coefficient `g(N_0)^4` is then exponentiated in double precision and does overflow in three cells, so the run emits `RuntimeWarning: overflow encountered in exp` and prints `inf` there, and prints `nan` for the column that violates (H) (see the E9b caveats). Two further `RuntimeWarning`s come from `scipy`'s `Radau` solver in Parts 3 and 4 when a solution leaves double range. Parts 3 and 4 use `scipy.integrate.solve_ivp` with the stiff `Radau` method; "overflow" in Part 3 means only that the solver could not reach that time because `y` left double range — it is **not** a blowup diagnostic (a globally existing `p = 1.00` solution overflows too), and the script's legend, which used to say otherwise, has been corrected.

## References

- J.-L. Lions, *Quelques méthodes de résolution des problèmes aux limites non linéaires*, Dunod/Gauthier-Villars, 1969 — global regularity for `α ≥ 5/4`. \[checked via WebSearch: book and the `α ≥ 5/4` attribution, via `00_problem.md` item 2.10; the proof in E1 is written independently and is not claimed to be Lions'.\] <!-- codespell:ignore methodes,problemes,resolution -->
- T. Tao, "Global regularity for a logarithmically supercritical hyperdissipative Navier-Stokes equation", Analysis & PDE 2 (2009) 361-366, arXiv:0906.3070. \[checked via WebSearch: venue, volume, pages, arXiv id, and the hypothesis `m(ξ) ≥ |ξ|^{(d+2)/4}/g(|ξ|)` with `∫_1^∞ ds/(s g(s)^4) = +∞` and the example `log^{1/4}(2+|ξ|^2)`.\]
- D. Barbato, F. Morandin, M. Romito, "Global regularity for a slightly supercritical hyperdissipative Navier-Stokes system", Analysis & PDE 7 (2014) 2009-2027, arXiv:1407.6734. \[checked via WebSearch: venue, volume, pages, arXiv id, the abstract phrase "under the optimal condition on the correction to the dissipation", and that it proves a conjecture of Tao whose form `∫_1^∞ ds/(s G(s)^2) = ∞` and heuristic justification were also returned in a snippet.\]
- D. Barbato, F. Morandin, M. Romito, "Global regularity for a logarithmically supercritical hyperdissipative dyadic equation", Dynamics of Partial Differential Equations 11 (2014) 39-52, arXiv:1403.2852. \[checked via WebSearch: title, venue, volume, pages, arXiv id, and the description of the method as an iterative estimate on the energy above shell `j` and the energy dissipated by those shells.\]
- M. Colombo, S. Haffter, "Global regularity for the hyperdissipative Navier-Stokes equation below the critical order", arXiv:1911.02600, J. Differential Equations (2020). \[checked via WebSearch: authors, title, arXiv id, and the abstract, including the `ε = ε(M,δ)` dependence and the openness statement in `H^{5/4} × (3/4, 5/4]`.\]
- A. Cheskidov, "Blow-up in finite time for the dyadic model of the Navier-Stokes equations", Trans. Amer. Math. Soc. 360 (2008) 5101-5120. \[checked via WebSearch: venue, volume, pages, and the threshold `α < 1/3`; the normalization behind that number was not verified.\]
- N. H. Katz, N. Pavlović, "Finite time blow-up for a dyadic model of the Navier-Stokes equations", Trans. Amer. Math. Soc. 357 (2005). [unverified; cited only as the origin of the shell model used in E9e.]
- T. Tao, "Finite time blowup for an averaged three-dimensional Navier-Stokes equation", J. Amer. Math. Soc. 29 (2016) 601-674, arXiv:1402.0290 — the source of transfer test T1; see `00_problem.md` item 2.2. \[checked via WebSearch, in `00_problem.md`.\]
- "Non-uniqueness of weak solutions for a logarithmically supercritical hyperdissipative Navier-Stokes system", arXiv:2406.05853. [checked via WebSearch: title and arXiv id only; authors and content not verified.]
- Kato-Ponce commutator estimate (K3) and the Sobolev product estimate (K2). [unverified as to exact references; both are standard and are used as black boxes.]
